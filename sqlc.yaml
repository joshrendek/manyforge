version: "2"
sql:
  - engine: postgresql
    schema: migrations
    queries: db/query
    gen:
      go:
        package: dbgen
        out: internal/platform/db/dbgen
        sql_package: pgx/v5
        emit_json_tags: true
        emit_pointers_for_null_types: true
        emit_interface: true
