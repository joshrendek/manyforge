/**
 * ManyForge API
 * Current implemented ManyForge application API. Configure an explicit instance-root URL. Management uses bearer access JWTs; public keys and server signatures have separate capabilities. Optional modules remain documented when disabled; their unavailable routes return real HTTP errors. Errors may be non-JSON at an intermediary; empty 202/204 responses contain no model. No SSE interface is implemented.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

// Focused OpenAPI Generator API template; native HTTP is owned by Transport.
import { Resource, PaginationError, type Transport, type RequestOptions, type ByteStream, type Upload } from '../transport.js';
import { decodeJSON, type JsonValue } from '../model-support.js';
import type { CreateEmailDomain } from '../models/CreateEmailDomain.js';
import type { EmailDomain } from '../models/EmailDomain.js';
import type { EmailDomainPage } from '../models/EmailDomainPage.js';


export interface BusinessInboxEmailDomainsCreateParams {

    body: CreateEmailDomain;

}


export interface BusinessInboxEmailDomainsListParams {

    cursor?: string;

    limit?: number;

}


export interface BusinessInboxEmailDomainsVerifyParams {

    did: string;

}


export class BusinessInboxEmailDomainsApi extends Resource {
    create(params: BusinessInboxEmailDomainsCreateParams, options?: RequestOptions): Promise<EmailDomain> {
        return this.transport.request({
            operationId: 'InboxEmailDomainsCreate',
            method: 'POST',
            path: '/api/v1/businesses/{id}/email-domains',
            pathParams: { ...this.scope,  },
            query: [],
            headers: {  },
            authenticated: true,
            audience: 'management',
            body: params.body,
            bodySchema: {"$ref":"#/components/schemas/CreateEmailDomain"},
            contentType: 'application/json',

            response: 'json',
            responseSchema: {"$ref":"#/components/schemas/EmailDomain"},
            decode: (text: string) => decodeJSON<EmailDomain>(text, {"$ref":"#/components/schemas/EmailDomain"}),
        }, options);
    }
    list(params: BusinessInboxEmailDomainsListParams = {}, options?: RequestOptions): Promise<EmailDomainPage> {
        return this.transport.request({
            operationId: 'InboxEmailDomainsList',
            method: 'GET',
            path: '/api/v1/businesses/{id}/email-domains',
            pathParams: { ...this.scope,  },
            query: [{ name: 'cursor', value: params.cursor, style: 'form', explode: true }, { name: 'limit', value: params.limit, style: 'form', explode: true }, ],
            headers: {  },
            authenticated: true,
            audience: 'management',

            response: 'json',
            responseSchema: {"$ref":"#/components/schemas/EmailDomainPage"},
            decode: (text: string) => decodeJSON<EmailDomainPage>(text, {"$ref":"#/components/schemas/EmailDomainPage"}),
        }, options);
    }

    async *iter(params: BusinessInboxEmailDomainsListParams = {}, options?: RequestOptions): AsyncIterable<EmailDomainPage['items'][number]> {
        let cursor = params.cursor;
        const seen = new Set<string>();
        if (cursor) seen.add(cursor);
        while (true) {
            const page = await this.list({ ...params, cursor }, options);
            for (const item of page.items) yield item;
            const next = page.nextCursor;
            if (!next) return;
            if (seen.has(next)) throw new PaginationError();
            seen.add(next);
            cursor = next;
        }
    }
    verify(params: BusinessInboxEmailDomainsVerifyParams, options?: RequestOptions): Promise<EmailDomain> {
        return this.transport.request({
            operationId: 'InboxEmailDomainsVerify',
            method: 'POST',
            path: '/api/v1/businesses/{id}/email-domains/{did}/verify',
            pathParams: { ...this.scope, 'did': String(params.did),  },
            query: [],
            headers: {  },
            authenticated: true,
            audience: 'management',

            response: 'json',
            responseSchema: {"$ref":"#/components/schemas/EmailDomain"},
            decode: (text: string) => decodeJSON<EmailDomain>(text, {"$ref":"#/components/schemas/EmailDomain"}),
        }, options);
    }
}
