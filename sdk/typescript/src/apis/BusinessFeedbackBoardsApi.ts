/**
 * ManyForge API
 * Current implemented ManyForge application API. Configure an explicit instance-root URL. Management uses bearer access JWTs; public keys and server signatures have separate capabilities. Optional modules remain documented when disabled; their unavailable routes return real HTTP errors. Errors may be non-JSON at an intermediary; empty 202/204 responses contain no model. No SSE interface is implemented.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

// Focused OpenAPI Generator API template; native HTTP is owned by Transport.
import { Resource, PaginationError, type Transport, type RequestOptions, type ByteStream, type Upload } from '../transport.js';
import { decodeJSON, type JsonValue } from '../model-support.js';
import type { Board } from '../models/Board.js';
import type { BoardCreate } from '../models/BoardCreate.js';
import type { BoardPage } from '../models/BoardPage.js';
import type { BoardUpdate } from '../models/BoardUpdate.js';


export interface BusinessFeedbackBoardsCreateParams {

    body: BoardCreate;

}


export interface BusinessFeedbackBoardsGetParams {

    bid: string;

}


export interface BusinessFeedbackBoardsListParams {

    cursor?: string;

    limit?: number;

}


export interface BusinessFeedbackBoardsUpdateParams {

    bid: string;

    body: BoardUpdate;

}


export class BusinessFeedbackBoardsApi extends Resource {
    create(params: BusinessFeedbackBoardsCreateParams, options?: RequestOptions): Promise<Board> {
        return this.transport.request({
            operationId: 'createFeedbackBoard',
            method: 'POST',
            path: '/api/v1/businesses/{id}/feedback/boards',
            pathParams: { ...this.scope,  },
            query: [],
            headers: {  },
            authenticated: true,
            audience: 'management',
            body: params.body,
            bodySchema: {"$ref":"#/components/schemas/BoardCreate"},
            contentType: 'application/json',

            response: 'json',
            responseSchema: {"$ref":"#/components/schemas/Board"},
            decode: (text: string) => decodeJSON<Board>(text, {"$ref":"#/components/schemas/Board"}),
        }, options);
    }
    get(params: BusinessFeedbackBoardsGetParams, options?: RequestOptions): Promise<Board> {
        return this.transport.request({
            operationId: 'getFeedbackBoard',
            method: 'GET',
            path: '/api/v1/businesses/{id}/feedback/boards/{bid}',
            pathParams: { ...this.scope, 'bid': String(params.bid),  },
            query: [],
            headers: {  },
            authenticated: true,
            audience: 'management',

            response: 'json',
            responseSchema: {"$ref":"#/components/schemas/Board"},
            decode: (text: string) => decodeJSON<Board>(text, {"$ref":"#/components/schemas/Board"}),
        }, options);
    }
    list(params: BusinessFeedbackBoardsListParams = {}, options?: RequestOptions): Promise<BoardPage> {
        return this.transport.request({
            operationId: 'listFeedbackBoards',
            method: 'GET',
            path: '/api/v1/businesses/{id}/feedback/boards',
            pathParams: { ...this.scope,  },
            query: [{ name: 'cursor', value: params.cursor, style: 'form', explode: true }, { name: 'limit', value: params.limit, style: 'form', explode: true }, ],
            headers: {  },
            authenticated: true,
            audience: 'management',

            response: 'json',
            responseSchema: {"$ref":"#/components/schemas/BoardPage"},
            decode: (text: string) => decodeJSON<BoardPage>(text, {"$ref":"#/components/schemas/BoardPage"}),
        }, options);
    }

    async *iter(params: BusinessFeedbackBoardsListParams = {}, options?: RequestOptions): AsyncIterable<BoardPage['items'][number]> {
        let cursor = params.cursor;
        const seen = new Set<string>();
        if (cursor) seen.add(cursor);
        while (true) {
            const page = await this.list({ ...params, cursor }, options);
            for (const item of page.items) yield item;
            const next = page.nextCursor;
            if (!next) return;
            if (seen.has(next)) throw new PaginationError();
            seen.add(next);
            cursor = next;
        }
    }
    update(params: BusinessFeedbackBoardsUpdateParams, options?: RequestOptions): Promise<Board> {
        return this.transport.request({
            operationId: 'updateFeedbackBoard',
            method: 'PATCH',
            path: '/api/v1/businesses/{id}/feedback/boards/{bid}',
            pathParams: { ...this.scope, 'bid': String(params.bid),  },
            query: [],
            headers: {  },
            authenticated: true,
            audience: 'management',
            body: params.body,
            bodySchema: {"$ref":"#/components/schemas/BoardUpdate"},
            contentType: 'application/json',

            response: 'json',
            responseSchema: {"$ref":"#/components/schemas/Board"},
            decode: (text: string) => decodeJSON<Board>(text, {"$ref":"#/components/schemas/Board"}),
        }, options);
    }
}
