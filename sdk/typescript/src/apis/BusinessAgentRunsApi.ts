/**
 * ManyForge API
 * Current implemented ManyForge application API. Configure an explicit instance-root URL. Management uses bearer access JWTs; public keys and server signatures have separate capabilities. Optional modules remain documented when disabled; their unavailable routes return real HTTP errors. Errors may be non-JSON at an intermediary; empty 202/204 responses contain no model. No SSE interface is implemented.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

// Focused OpenAPI Generator API template; native HTTP is owned by Transport.
import { Resource, PaginationError, type Transport, type RequestOptions, type ByteStream, type Upload } from '../transport.js';
import { decodeJSON, type JsonValue } from '../model-support.js';
import type { AgentRunPage } from '../models/AgentRunPage.js';
import type { Run } from '../models/Run.js';
import type { TriggerRunRequest } from '../models/TriggerRunRequest.js';


export interface BusinessAgentRunsGetParams {

    agentID: string;

    runID: string;

}


export interface BusinessAgentRunsListParams {

    agentID: string;

    status?: string;

    window?: ListAgentRunsWindowEnum;

    from?: string;

    to?: string;

    cursor?: string;

    limit?: bigint;

}

export const ListAgentRunsWindowEnum = {
    ThisMonth: 'this_month',
    LastMonth: 'last_month',
    Last30Days: 'last_30_days',
    Custom: 'custom',

} as const;
export type ListAgentRunsWindowEnum = typeof ListAgentRunsWindowEnum[keyof typeof ListAgentRunsWindowEnum] | (string & {});


export interface BusinessAgentRunsCreateParams {

    agentID: string;

    body?: TriggerRunRequest;

}


export class BusinessAgentRunsApi extends Resource {
    get(params: BusinessAgentRunsGetParams, options?: RequestOptions): Promise<Run> {
        return this.transport.request({
            operationId: 'getAgentRun',
            method: 'GET',
            path: '/api/v1/businesses/{id}/agents/{agentID}/runs/{runID}',
            pathParams: { ...this.scope, 'agentID': String(params.agentID), 'runID': String(params.runID),  },
            query: [],
            headers: {  },
            authenticated: true,
            audience: 'management',

            response: 'json',
            responseSchema: {"$ref":"#/components/schemas/Run"},
            decode: (text: string) => decodeJSON<Run>(text, {"$ref":"#/components/schemas/Run"}),
        }, options);
    }
    list(params: BusinessAgentRunsListParams, options?: RequestOptions): Promise<AgentRunPage> {
        return this.transport.request({
            operationId: 'listAgentRuns',
            method: 'GET',
            path: '/api/v1/businesses/{id}/agents/{agentID}/runs',
            pathParams: { ...this.scope, 'agentID': String(params.agentID),  },
            query: [{ name: 'status', value: params.status, style: 'form', explode: true }, { name: 'window', value: params.window, style: 'form', explode: true }, { name: 'from', value: params.from, style: 'form', explode: true }, { name: 'to', value: params.to, style: 'form', explode: true }, { name: 'cursor', value: params.cursor, style: 'form', explode: true }, { name: 'limit', value: params.limit, style: 'form', explode: true }, ],
            headers: {  },
            authenticated: true,
            audience: 'management',

            response: 'json',
            responseSchema: {"$ref":"#/components/schemas/AgentRunPage"},
            decode: (text: string) => decodeJSON<AgentRunPage>(text, {"$ref":"#/components/schemas/AgentRunPage"}),
        }, options);
    }

    async *iter(params: BusinessAgentRunsListParams, options?: RequestOptions): AsyncIterable<AgentRunPage['items'][number]> {
        let cursor = params.cursor;
        const seen = new Set<string>();
        if (cursor) seen.add(cursor);
        while (true) {
            const page = await this.list({ ...params, cursor }, options);
            for (const item of page.items) yield item;
            const next = page.nextCursor;
            if (!next) return;
            if (seen.has(next)) throw new PaginationError();
            seen.add(next);
            cursor = next;
        }
    }
    create(params: BusinessAgentRunsCreateParams, options?: RequestOptions): Promise<Run> {
        return this.transport.request({
            operationId: 'triggerAgentRun',
            method: 'POST',
            path: '/api/v1/businesses/{id}/agents/{agentID}/runs',
            pathParams: { ...this.scope, 'agentID': String(params.agentID),  },
            query: [],
            headers: {  },
            authenticated: true,
            audience: 'management',
            body: params.body,
            bodySchema: {"$ref":"#/components/schemas/TriggerRunRequest"},
            contentType: 'application/json',

            response: 'json',
            responseSchema: {"$ref":"#/components/schemas/Run"},
            decode: (text: string) => decodeJSON<Run>(text, {"$ref":"#/components/schemas/Run"}),
        }, options);
    }
}
