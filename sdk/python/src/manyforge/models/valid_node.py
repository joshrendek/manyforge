"""
    ManyForge API

    Current implemented ManyForge application API. Configure an explicit instance-root URL. Management uses bearer access JWTs; public keys and server signatures have separate capabilities. Optional modules remain documented when disabled; their unavailable routes return real HTTP errors. Errors may be non-JSON at an intermediary; empty 202/204 responses contain no model. No SSE interface is implemented.

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
from datetime import date, datetime
from uuid import UUID
from typing import Any, ClassVar, Dict, List, Optional, Union
from pydantic import Field, StrictBool, StrictBytes, StrictFloat, StrictInt, StrictStr, RootModel, field_validator, model_validator
from manyforge.model_support import Model, OpenEnum, JsonValue, UNSET, UnsetType

from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from manyforge.models.add_tag_node import AddTagNode
from manyforge.models.condition_node import ConditionNode
from manyforge.models.exit_node import ExitNode
from manyforge.models.remove_tag_node import RemoveTagNode
from manyforge.models.send_email_node import SendEmailNode
from manyforge.models.trigger_node import TriggerNode
from manyforge.models.wait_node import WaitNode

class ValidNode(RootModel[Union[AddTagNode, ConditionNode, ExitNode, RemoveTagNode, SendEmailNode, TriggerNode, WaitNode, ]]):
    """Typed wire union; serialize its root, never a wrapper object."""
    @model_validator(mode="before")
    @classmethod
    def select_variant(cls, value: Any) -> Any:
        if not isinstance(value, dict):
            return value
        if value.get("kind") == "add_tag":
            return AddTagNode.model_validate(value)
        if value.get("kind") == "condition":
            return ConditionNode.model_validate(value)
        if value.get("kind") == "exit":
            return ExitNode.model_validate(value)
        if value.get("kind") == "remove_tag":
            return RemoveTagNode.model_validate(value)
        if value.get("kind") == "send_email":
            return SendEmailNode.model_validate(value)
        if value.get("kind") == "trigger":
            return TriggerNode.model_validate(value)
        if value.get("kind") == "wait":
            return WaitNode.model_validate(value)
        return value



