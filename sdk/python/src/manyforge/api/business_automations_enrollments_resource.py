"""
    ManyForge API

    Current implemented ManyForge application API. Configure an explicit instance-root URL. Management uses bearer access JWTs; public keys and server signatures have separate capabilities. Optional modules remain documented when disabled; their unavailable routes return real HTTP errors. Errors may be non-JSON at an intermediary; empty 202/204 responses contain no model. No SSE interface is implemented.

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
from datetime import date, datetime
from typing import Any, Dict, List, Optional, Union
from collections.abc import Iterator, AsyncIterator, Mapping
from pydantic import TypeAdapter, StrictBool, StrictBytes, StrictFloat, StrictInt, StrictStr
from manyforge.model_support import JsonValue, UNSET, UnsetType
from manyforge.transport import (SyncTransport, AsyncTransport, Request, RequestOptions, QueryParameter, Upload, ByteStream, AsyncByteStream, PaginationError)
from pydantic import Field, StrictInt, StrictStr, field_validator
from typing import Optional
from typing_extensions import Annotated
from uuid import UUID
from manyforge.models.automation_enrollment_input import AutomationEnrollmentInput
from manyforge.models.enrollment import Enrollment
from manyforge.models.enrollment_detail import EnrollmentDetail
from manyforge.models.enrollment_page import EnrollmentPage
from manyforge.models.list_automation_enrollments_status_enum import ListAutomationEnrollmentsStatusEnum

from manyforge.models import Enrollment

class BusinessAutomationsEnrollmentsResource:
    def __init__(self, transport: SyncTransport, bindings: Mapping[str, str] | None = None) -> None:
        self._transport = transport
        self._bindings = dict(bindings or {})

    def create(
        self,
        *,

        aid: UUID,

        automation_enrollment_input: AutomationEnrollmentInput,

        request_options: RequestOptions | None = None,
    ) -> Enrollment:
        request = Request(
            operation_id="enrollAutomationSubscriber",
            method="POST",
            path="/api/v1/businesses/{id}/mailing/automations/{aid}/enrollments",
            authenticated=True,
            response=TypeAdapter(Enrollment),
            path_parameters={
                "id": self._bindings["id"],
                "aid": aid,
            },
            query=(
            ),
            headers={
            },
            body=automation_enrollment_input,
            form={
            },

            content_type="application/json",


            accept="application/json",

        )
        return self._transport.request(request, request_options=request_options)

    def exit(
        self,
        *,

        aid: UUID,

        eid: UUID,

        request_options: RequestOptions | None = None,
    ) -> Enrollment:
        request = Request(
            operation_id="exitAutomationEnrollment",
            method="POST",
            path="/api/v1/businesses/{id}/mailing/automations/{aid}/enrollments/{eid}/exit",
            authenticated=True,
            response=TypeAdapter(Enrollment),
            path_parameters={
                "id": self._bindings["id"],
                "aid": aid,
                "eid": eid,
            },
            query=(
            ),
            headers={
            },
            form={
            },


            accept="application/json",

        )
        return self._transport.request(request, request_options=request_options)

    def get(
        self,
        *,

        aid: UUID,

        eid: UUID,

        request_options: RequestOptions | None = None,
    ) -> EnrollmentDetail:
        request = Request(
            operation_id="getAutomationEnrollment",
            method="GET",
            path="/api/v1/businesses/{id}/mailing/automations/{aid}/enrollments/{eid}",
            authenticated=True,
            response=TypeAdapter(EnrollmentDetail),
            path_parameters={
                "id": self._bindings["id"],
                "aid": aid,
                "eid": eid,
            },
            query=(
            ),
            headers={
            },
            form={
            },


            accept="application/json",

        )
        return self._transport.request(request, request_options=request_options)

    def list(
        self,
        *,

        aid: UUID,

        status: ListAutomationEnrollmentsStatusEnum | UnsetType = UNSET,

        node_id: str | UnsetType = UNSET,

        cursor: str | UnsetType = UNSET,

        limit: int | UnsetType = UNSET,

        request_options: RequestOptions | None = None,
    ) -> EnrollmentPage:
        request = Request(
            operation_id="listAutomationEnrollments",
            method="GET",
            path="/api/v1/businesses/{id}/mailing/automations/{aid}/enrollments",
            authenticated=True,
            response=TypeAdapter(EnrollmentPage),
            path_parameters={
                "id": self._bindings["id"],
                "aid": aid,
            },
            query=(
                QueryParameter("status", status, "form", True),
                QueryParameter("node_id", node_id, "form", True),
                QueryParameter("cursor", cursor, "form", True),
                QueryParameter("limit", limit, "form", True),
            ),
            headers={
            },
            form={
            },


            accept="application/json",

        )
        return self._transport.request(request, request_options=request_options)

    def iter(
        self,
        *,

        aid: UUID,

        status: ListAutomationEnrollmentsStatusEnum | UnsetType = UNSET,

        node_id: str | UnsetType = UNSET,

        cursor: str | UnsetType = UNSET,

        limit: int | UnsetType = UNSET,

        request_options: RequestOptions | None = None,
    ) -> Iterator[Enrollment]:
        seen: set[str] = set()
        next_cursor = cursor
        if isinstance(next_cursor, str) and next_cursor:
            seen.add(next_cursor)
        while True:
            page = self.list(

                aid=aid,

                status=status,

                node_id=node_id,

                cursor=next_cursor,

                limit=limit,

                request_options=request_options,
            )
            for item in page.items:
                yield item
            next_cursor = page.next_cursor
            if isinstance(next_cursor, UnsetType) or not next_cursor:
                return
            if next_cursor in seen:
                raise PaginationError("Server returned a repeated cursor")
            seen.add(next_cursor)

class AsyncBusinessAutomationsEnrollmentsResource:
    def __init__(self, transport: AsyncTransport, bindings: Mapping[str, str] | None = None) -> None:
        self._transport = transport
        self._bindings = dict(bindings or {})

    async def create(
        self,
        *,

        aid: UUID,

        automation_enrollment_input: AutomationEnrollmentInput,

        request_options: RequestOptions | None = None,
    ) -> Enrollment:
        request = Request(
            operation_id="enrollAutomationSubscriber",
            method="POST",
            path="/api/v1/businesses/{id}/mailing/automations/{aid}/enrollments",
            authenticated=True,
            response=TypeAdapter(Enrollment),
            path_parameters={
                "id": self._bindings["id"],
                "aid": aid,
            },
            query=(
            ),
            headers={
            },
            body=automation_enrollment_input,
            form={
            },

            content_type="application/json",


            accept="application/json",

        )
        return await self._transport.request(request, request_options=request_options)

    async def exit(
        self,
        *,

        aid: UUID,

        eid: UUID,

        request_options: RequestOptions | None = None,
    ) -> Enrollment:
        request = Request(
            operation_id="exitAutomationEnrollment",
            method="POST",
            path="/api/v1/businesses/{id}/mailing/automations/{aid}/enrollments/{eid}/exit",
            authenticated=True,
            response=TypeAdapter(Enrollment),
            path_parameters={
                "id": self._bindings["id"],
                "aid": aid,
                "eid": eid,
            },
            query=(
            ),
            headers={
            },
            form={
            },


            accept="application/json",

        )
        return await self._transport.request(request, request_options=request_options)

    async def get(
        self,
        *,

        aid: UUID,

        eid: UUID,

        request_options: RequestOptions | None = None,
    ) -> EnrollmentDetail:
        request = Request(
            operation_id="getAutomationEnrollment",
            method="GET",
            path="/api/v1/businesses/{id}/mailing/automations/{aid}/enrollments/{eid}",
            authenticated=True,
            response=TypeAdapter(EnrollmentDetail),
            path_parameters={
                "id": self._bindings["id"],
                "aid": aid,
                "eid": eid,
            },
            query=(
            ),
            headers={
            },
            form={
            },


            accept="application/json",

        )
        return await self._transport.request(request, request_options=request_options)

    async def list(
        self,
        *,

        aid: UUID,

        status: ListAutomationEnrollmentsStatusEnum | UnsetType = UNSET,

        node_id: str | UnsetType = UNSET,

        cursor: str | UnsetType = UNSET,

        limit: int | UnsetType = UNSET,

        request_options: RequestOptions | None = None,
    ) -> EnrollmentPage:
        request = Request(
            operation_id="listAutomationEnrollments",
            method="GET",
            path="/api/v1/businesses/{id}/mailing/automations/{aid}/enrollments",
            authenticated=True,
            response=TypeAdapter(EnrollmentPage),
            path_parameters={
                "id": self._bindings["id"],
                "aid": aid,
            },
            query=(
                QueryParameter("status", status, "form", True),
                QueryParameter("node_id", node_id, "form", True),
                QueryParameter("cursor", cursor, "form", True),
                QueryParameter("limit", limit, "form", True),
            ),
            headers={
            },
            form={
            },


            accept="application/json",

        )
        return await self._transport.request(request, request_options=request_options)

    async def iter(
        self,
        *,

        aid: UUID,

        status: ListAutomationEnrollmentsStatusEnum | UnsetType = UNSET,

        node_id: str | UnsetType = UNSET,

        cursor: str | UnsetType = UNSET,

        limit: int | UnsetType = UNSET,

        request_options: RequestOptions | None = None,
    ) -> AsyncIterator[Enrollment]:
        seen: set[str] = set()
        next_cursor = cursor
        if isinstance(next_cursor, str) and next_cursor:
            seen.add(next_cursor)
        while True:
            page = await self.list(

                aid=aid,

                status=status,

                node_id=node_id,

                cursor=next_cursor,

                limit=limit,

                request_options=request_options,
            )
            for item in page.items:
                yield item
            next_cursor = page.next_cursor
            if isinstance(next_cursor, UnsetType) or not next_cursor:
                return
            if next_cursor in seen:
                raise PaginationError("Server returned a repeated cursor")
            seen.add(next_cursor)

