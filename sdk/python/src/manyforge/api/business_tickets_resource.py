"""
    ManyForge API

    Current implemented ManyForge application API. Configure an explicit instance-root URL. Management uses bearer access JWTs; public keys and server signatures have separate capabilities. Optional modules remain documented when disabled; their unavailable routes return real HTTP errors. Errors may be non-JSON at an intermediary; empty 202/204 responses contain no model. No SSE interface is implemented.

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
from datetime import date, datetime
from typing import Any, Dict, List, Optional, Union
from collections.abc import Iterator, AsyncIterator, Mapping
from pydantic import TypeAdapter, StrictBool, StrictBytes, StrictFloat, StrictInt, StrictStr
from manyforge.model_support import JsonValue, UNSET, UnsetType
from manyforge.transport import (SyncTransport, AsyncTransport, Request, RequestOptions, QueryParameter, Upload, ByteStream, AsyncByteStream, PaginationError)
from pydantic import Field, StrictStr
from typing import Optional
from typing_extensions import Annotated
from uuid import UUID
from manyforge.models.note_body import NoteBody
from manyforge.models.patch_ticket import PatchTicket
from manyforge.models.reply_body import ReplyBody
from manyforge.models.ticket import Ticket
from manyforge.models.ticket_message import TicketMessage
from manyforge.models.ticket_page import TicketPage
from manyforge.models.ticket_priority import TicketPriority
from manyforge.models.ticket_status import TicketStatus

from manyforge.models import Ticket

class BusinessTicketsResource:
    def __init__(self, transport: SyncTransport, bindings: Mapping[str, str] | None = None) -> None:
        self._transport = transport
        self._bindings = dict(bindings or {})

    def add_note(
        self,
        *,

        tid: UUID,

        note_body: NoteBody,

        request_options: RequestOptions | None = None,
    ) -> TicketMessage:
        request = Request(
            operation_id="TicketsAddNote",
            method="POST",
            path="/api/v1/businesses/{id}/tickets/{tid}/note",
            authenticated=True,
            response=TypeAdapter(TicketMessage),
            path_parameters={
                "id": self._bindings["id"],
                "tid": tid,
            },
            query=(
            ),
            headers={
            },
            body=note_body,
            form={
            },

            content_type="application/json",


            accept="application/json",

        )
        return self._transport.request(request, request_options=request_options)

    def delete(
        self,
        *,

        tid: UUID,

        request_options: RequestOptions | None = None,
    ) -> None:
        request = Request(
            operation_id="TicketsDelete",
            method="DELETE",
            path="/api/v1/businesses/{id}/tickets/{tid}",
            authenticated=True,
            response=TypeAdapter(type(None)),
            path_parameters={
                "id": self._bindings["id"],
                "tid": tid,
            },
            query=(
            ),
            headers={
            },
            form={
            },


            accept="application/json",

        )
        return self._transport.request(request, request_options=request_options)

    def get(
        self,
        *,

        tid: UUID,

        request_options: RequestOptions | None = None,
    ) -> Ticket:
        request = Request(
            operation_id="TicketsGet",
            method="GET",
            path="/api/v1/businesses/{id}/tickets/{tid}",
            authenticated=True,
            response=TypeAdapter(Ticket),
            path_parameters={
                "id": self._bindings["id"],
                "tid": tid,
            },
            query=(
            ),
            headers={
            },
            form={
            },


            accept="application/json",

        )
        return self._transport.request(request, request_options=request_options)

    def list(
        self,
        *,

        cursor: str | UnsetType = UNSET,

        limit: int | UnsetType = UNSET,

        status: TicketStatus | UnsetType = UNSET,

        priority: TicketPriority | UnsetType = UNSET,

        assignee: str | UnsetType = UNSET,

        tag: str | UnsetType = UNSET,

        search: str | UnsetType = UNSET,

        request_options: RequestOptions | None = None,
    ) -> TicketPage:
        request = Request(
            operation_id="TicketsList",
            method="GET",
            path="/api/v1/businesses/{id}/tickets",
            authenticated=True,
            response=TypeAdapter(TicketPage),
            path_parameters={
                "id": self._bindings["id"],
            },
            query=(
                QueryParameter("cursor", cursor, "form", True),
                QueryParameter("limit", limit, "form", True),
                QueryParameter("status", status, "form", True),
                QueryParameter("priority", priority, "form", True),
                QueryParameter("assignee", assignee, "form", True),
                QueryParameter("tag", tag, "form", True),
                QueryParameter("search", search, "form", True),
            ),
            headers={
            },
            form={
            },


            accept="application/json",

        )
        return self._transport.request(request, request_options=request_options)

    def iter(
        self,
        *,

        cursor: str | UnsetType = UNSET,

        limit: int | UnsetType = UNSET,

        status: TicketStatus | UnsetType = UNSET,

        priority: TicketPriority | UnsetType = UNSET,

        assignee: str | UnsetType = UNSET,

        tag: str | UnsetType = UNSET,

        search: str | UnsetType = UNSET,

        request_options: RequestOptions | None = None,
    ) -> Iterator[Ticket]:
        seen: set[str] = set()
        next_cursor = cursor
        if isinstance(next_cursor, str) and next_cursor:
            seen.add(next_cursor)
        while True:
            page = self.list(

                cursor=next_cursor,

                limit=limit,

                status=status,

                priority=priority,

                assignee=assignee,

                tag=tag,

                search=search,

                request_options=request_options,
            )
            for item in page.items:
                yield item
            next_cursor = page.next_cursor
            if isinstance(next_cursor, UnsetType) or not next_cursor:
                return
            if next_cursor in seen:
                raise PaginationError("Server returned a repeated cursor")
            seen.add(next_cursor)

    def reply(
        self,
        *,

        tid: UUID,

        reply_body: ReplyBody,

        request_options: RequestOptions | None = None,
    ) -> TicketMessage:
        request = Request(
            operation_id="TicketsReply",
            method="POST",
            path="/api/v1/businesses/{id}/tickets/{tid}/reply",
            authenticated=True,
            response=TypeAdapter(TicketMessage),
            path_parameters={
                "id": self._bindings["id"],
                "tid": tid,
            },
            query=(
            ),
            headers={
            },
            body=reply_body,
            form={
            },

            content_type="application/json",


            accept="application/json",

        )
        return self._transport.request(request, request_options=request_options)

    def update(
        self,
        *,

        tid: UUID,

        patch_ticket: PatchTicket,

        request_options: RequestOptions | None = None,
    ) -> Ticket:
        request = Request(
            operation_id="TicketsUpdate",
            method="PATCH",
            path="/api/v1/businesses/{id}/tickets/{tid}",
            authenticated=True,
            response=TypeAdapter(Ticket),
            path_parameters={
                "id": self._bindings["id"],
                "tid": tid,
            },
            query=(
            ),
            headers={
            },
            body=patch_ticket,
            form={
            },

            content_type="application/json",


            accept="application/json",

        )
        return self._transport.request(request, request_options=request_options)

class AsyncBusinessTicketsResource:
    def __init__(self, transport: AsyncTransport, bindings: Mapping[str, str] | None = None) -> None:
        self._transport = transport
        self._bindings = dict(bindings or {})

    async def add_note(
        self,
        *,

        tid: UUID,

        note_body: NoteBody,

        request_options: RequestOptions | None = None,
    ) -> TicketMessage:
        request = Request(
            operation_id="TicketsAddNote",
            method="POST",
            path="/api/v1/businesses/{id}/tickets/{tid}/note",
            authenticated=True,
            response=TypeAdapter(TicketMessage),
            path_parameters={
                "id": self._bindings["id"],
                "tid": tid,
            },
            query=(
            ),
            headers={
            },
            body=note_body,
            form={
            },

            content_type="application/json",


            accept="application/json",

        )
        return await self._transport.request(request, request_options=request_options)

    async def delete(
        self,
        *,

        tid: UUID,

        request_options: RequestOptions | None = None,
    ) -> None:
        request = Request(
            operation_id="TicketsDelete",
            method="DELETE",
            path="/api/v1/businesses/{id}/tickets/{tid}",
            authenticated=True,
            response=TypeAdapter(type(None)),
            path_parameters={
                "id": self._bindings["id"],
                "tid": tid,
            },
            query=(
            ),
            headers={
            },
            form={
            },


            accept="application/json",

        )
        return await self._transport.request(request, request_options=request_options)

    async def get(
        self,
        *,

        tid: UUID,

        request_options: RequestOptions | None = None,
    ) -> Ticket:
        request = Request(
            operation_id="TicketsGet",
            method="GET",
            path="/api/v1/businesses/{id}/tickets/{tid}",
            authenticated=True,
            response=TypeAdapter(Ticket),
            path_parameters={
                "id": self._bindings["id"],
                "tid": tid,
            },
            query=(
            ),
            headers={
            },
            form={
            },


            accept="application/json",

        )
        return await self._transport.request(request, request_options=request_options)

    async def list(
        self,
        *,

        cursor: str | UnsetType = UNSET,

        limit: int | UnsetType = UNSET,

        status: TicketStatus | UnsetType = UNSET,

        priority: TicketPriority | UnsetType = UNSET,

        assignee: str | UnsetType = UNSET,

        tag: str | UnsetType = UNSET,

        search: str | UnsetType = UNSET,

        request_options: RequestOptions | None = None,
    ) -> TicketPage:
        request = Request(
            operation_id="TicketsList",
            method="GET",
            path="/api/v1/businesses/{id}/tickets",
            authenticated=True,
            response=TypeAdapter(TicketPage),
            path_parameters={
                "id": self._bindings["id"],
            },
            query=(
                QueryParameter("cursor", cursor, "form", True),
                QueryParameter("limit", limit, "form", True),
                QueryParameter("status", status, "form", True),
                QueryParameter("priority", priority, "form", True),
                QueryParameter("assignee", assignee, "form", True),
                QueryParameter("tag", tag, "form", True),
                QueryParameter("search", search, "form", True),
            ),
            headers={
            },
            form={
            },


            accept="application/json",

        )
        return await self._transport.request(request, request_options=request_options)

    async def iter(
        self,
        *,

        cursor: str | UnsetType = UNSET,

        limit: int | UnsetType = UNSET,

        status: TicketStatus | UnsetType = UNSET,

        priority: TicketPriority | UnsetType = UNSET,

        assignee: str | UnsetType = UNSET,

        tag: str | UnsetType = UNSET,

        search: str | UnsetType = UNSET,

        request_options: RequestOptions | None = None,
    ) -> AsyncIterator[Ticket]:
        seen: set[str] = set()
        next_cursor = cursor
        if isinstance(next_cursor, str) and next_cursor:
            seen.add(next_cursor)
        while True:
            page = await self.list(

                cursor=next_cursor,

                limit=limit,

                status=status,

                priority=priority,

                assignee=assignee,

                tag=tag,

                search=search,

                request_options=request_options,
            )
            for item in page.items:
                yield item
            next_cursor = page.next_cursor
            if isinstance(next_cursor, UnsetType) or not next_cursor:
                return
            if next_cursor in seen:
                raise PaginationError("Server returned a repeated cursor")
            seen.add(next_cursor)

    async def reply(
        self,
        *,

        tid: UUID,

        reply_body: ReplyBody,

        request_options: RequestOptions | None = None,
    ) -> TicketMessage:
        request = Request(
            operation_id="TicketsReply",
            method="POST",
            path="/api/v1/businesses/{id}/tickets/{tid}/reply",
            authenticated=True,
            response=TypeAdapter(TicketMessage),
            path_parameters={
                "id": self._bindings["id"],
                "tid": tid,
            },
            query=(
            ),
            headers={
            },
            body=reply_body,
            form={
            },

            content_type="application/json",


            accept="application/json",

        )
        return await self._transport.request(request, request_options=request_options)

    async def update(
        self,
        *,

        tid: UUID,

        patch_ticket: PatchTicket,

        request_options: RequestOptions | None = None,
    ) -> Ticket:
        request = Request(
            operation_id="TicketsUpdate",
            method="PATCH",
            path="/api/v1/businesses/{id}/tickets/{tid}",
            authenticated=True,
            response=TypeAdapter(Ticket),
            path_parameters={
                "id": self._bindings["id"],
                "tid": tid,
            },
            query=(
            ),
            headers={
            },
            body=patch_ticket,
            form={
            },

            content_type="application/json",


            accept="application/json",

        )
        return await self._transport.request(request, request_options=request_options)

