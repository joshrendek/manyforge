"""
    ManyForge API

    Current implemented ManyForge application API. Configure an explicit instance-root URL. Management uses bearer access JWTs; public keys and server signatures have separate capabilities. Optional modules remain documented when disabled; their unavailable routes return real HTTP errors. Errors may be non-JSON at an intermediary; empty 202/204 responses contain no model. No SSE interface is implemented.

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
from datetime import date, datetime
from typing import Any, Dict, List, Optional, Union
from collections.abc import Iterator, AsyncIterator, Mapping
from pydantic import TypeAdapter, StrictBool, StrictBytes, StrictFloat, StrictInt, StrictStr
from manyforge.model_support import JsonValue, UNSET, UnsetType
from manyforge.transport import (SyncTransport, AsyncTransport, Request, RequestOptions, QueryParameter, Upload, ByteStream, AsyncByteStream, PaginationError)
from pydantic import Field, StrictBool, StrictBytes, StrictInt, StrictStr
from typing import Optional, Tuple, Union
from typing_extensions import Annotated
from uuid import UUID
from manyforge.models.contacts_import import ContactsImport
from manyforge.models.import_result import ImportResult
from manyforge.models.subscriber import Subscriber
from manyforge.models.subscriber_input import SubscriberInput
from manyforge.models.subscriber_page import SubscriberPage
from manyforge.models.subscriber_patch import SubscriberPatch
from manyforge.models.subscriber_status import SubscriberStatus

from manyforge.models import Subscriber

class BusinessMailingSubscribersResource:
    def __init__(self, transport: SyncTransport, bindings: Mapping[str, str] | None = None) -> None:
        self._transport = transport
        self._bindings = dict(bindings or {})

    def from_contacts(
        self,
        *,

        lid: UUID,

        contacts_import: ContactsImport,

        request_options: RequestOptions | None = None,
    ) -> ImportResult:
        request = Request(
            operation_id="addMailingSubscribersFromContacts",
            method="POST",
            path="/api/v1/businesses/{id}/mailing/lists/{lid}/subscribers/from-contacts",
            authenticated=True,
            response=TypeAdapter(ImportResult),
            path_parameters={
                "id": self._bindings["id"],
                "lid": lid,
            },
            query=(
            ),
            headers={
            },
            body=contacts_import,
            form={
            },

            content_type="application/json",


            accept="application/json",

        )
        return self._transport.request(request, request_options=request_options)

    def create(
        self,
        *,

        lid: UUID,

        subscriber_input: SubscriberInput,

        request_options: RequestOptions | None = None,
    ) -> Subscriber:
        request = Request(
            operation_id="createMailingSubscriber",
            method="POST",
            path="/api/v1/businesses/{id}/mailing/lists/{lid}/subscribers",
            authenticated=True,
            response=TypeAdapter(Subscriber),
            path_parameters={
                "id": self._bindings["id"],
                "lid": lid,
            },
            query=(
            ),
            headers={
            },
            body=subscriber_input,
            form={
            },

            content_type="application/json",


            accept="application/json",

        )
        return self._transport.request(request, request_options=request_options)

    def export_csv(
        self,
        *,

        lid: UUID,

        request_options: RequestOptions | None = None,
    ) -> ByteStream:
        request = Request(
            operation_id="exportMailingSubscribersCSV",
            method="GET",
            path="/api/v1/businesses/{id}/mailing/lists/{lid}/subscribers/export",
            authenticated=True,
            response=TypeAdapter(type(None)),
            path_parameters={
                "id": self._bindings["id"],
                "lid": lid,
            },
            query=(
            ),
            headers={
            },
            form={
            },


            accept="text/csv",

        )
        return self._transport.stream(request, request_options=request_options)

    def get(
        self,
        *,

        lid: UUID,

        sid: UUID,

        request_options: RequestOptions | None = None,
    ) -> Subscriber:
        request = Request(
            operation_id="getMailingSubscriber",
            method="GET",
            path="/api/v1/businesses/{id}/mailing/lists/{lid}/subscribers/{sid}",
            authenticated=True,
            response=TypeAdapter(Subscriber),
            path_parameters={
                "id": self._bindings["id"],
                "lid": lid,
                "sid": sid,
            },
            query=(
            ),
            headers={
            },
            form={
            },


            accept="application/json",

        )
        return self._transport.request(request, request_options=request_options)

    def import_csv(
        self,
        *,

        lid: UUID,

        consent_attested: bool,

        file: Upload,

        skip_confirmation: bool | UnsetType = UNSET,

        request_options: RequestOptions | None = None,
    ) -> ImportResult:
        request = Request(
            operation_id="importMailingSubscribersCSV",
            method="POST",
            path="/api/v1/businesses/{id}/mailing/lists/{lid}/subscribers/import",
            authenticated=True,
            response=TypeAdapter(ImportResult),
            path_parameters={
                "id": self._bindings["id"],
                "lid": lid,
            },
            query=(
            ),
            headers={
            },
            form={
                "consent_attested": consent_attested,
                "file": file,
                "skip_confirmation": skip_confirmation,
            },

            content_type="multipart/form-data",


            accept="application/json",

        )
        return self._transport.request(request, request_options=request_options)

    def list(
        self,
        *,

        lid: UUID,

        q: str | UnsetType = UNSET,

        status: SubscriberStatus | UnsetType = UNSET,

        tag: str | UnsetType = UNSET,

        cursor: str | UnsetType = UNSET,

        limit: int | UnsetType = UNSET,

        request_options: RequestOptions | None = None,
    ) -> SubscriberPage:
        request = Request(
            operation_id="listMailingSubscribers",
            method="GET",
            path="/api/v1/businesses/{id}/mailing/lists/{lid}/subscribers",
            authenticated=True,
            response=TypeAdapter(SubscriberPage),
            path_parameters={
                "id": self._bindings["id"],
                "lid": lid,
            },
            query=(
                QueryParameter("q", q, "form", True),
                QueryParameter("status", status, "form", True),
                QueryParameter("tag", tag, "form", True),
                QueryParameter("cursor", cursor, "form", True),
                QueryParameter("limit", limit, "form", True),
            ),
            headers={
            },
            form={
            },


            accept="application/json",

        )
        return self._transport.request(request, request_options=request_options)

    def iter(
        self,
        *,

        lid: UUID,

        q: str | UnsetType = UNSET,

        status: SubscriberStatus | UnsetType = UNSET,

        tag: str | UnsetType = UNSET,

        cursor: str | UnsetType = UNSET,

        limit: int | UnsetType = UNSET,

        request_options: RequestOptions | None = None,
    ) -> Iterator[Subscriber]:
        seen: set[str] = set()
        next_cursor = cursor
        if isinstance(next_cursor, str) and next_cursor:
            seen.add(next_cursor)
        while True:
            page = self.list(

                lid=lid,

                q=q,

                status=status,

                tag=tag,

                cursor=next_cursor,

                limit=limit,

                request_options=request_options,
            )
            for item in page.items:
                yield item
            next_cursor = page.next_cursor
            if isinstance(next_cursor, UnsetType) or not next_cursor:
                return
            if next_cursor in seen:
                raise PaginationError("Server returned a repeated cursor")
            seen.add(next_cursor)

    def delete(
        self,
        *,

        lid: UUID,

        sid: UUID,

        request_options: RequestOptions | None = None,
    ) -> None:
        request = Request(
            operation_id="unsubscribeMailingSubscriber",
            method="DELETE",
            path="/api/v1/businesses/{id}/mailing/lists/{lid}/subscribers/{sid}",
            authenticated=True,
            response=TypeAdapter(type(None)),
            path_parameters={
                "id": self._bindings["id"],
                "lid": lid,
                "sid": sid,
            },
            query=(
            ),
            headers={
            },
            form={
            },


            accept="application/json",

        )
        return self._transport.request(request, request_options=request_options)

    def update(
        self,
        *,

        lid: UUID,

        sid: UUID,

        subscriber_patch: SubscriberPatch,

        request_options: RequestOptions | None = None,
    ) -> Subscriber:
        request = Request(
            operation_id="updateMailingSubscriber",
            method="PATCH",
            path="/api/v1/businesses/{id}/mailing/lists/{lid}/subscribers/{sid}",
            authenticated=True,
            response=TypeAdapter(Subscriber),
            path_parameters={
                "id": self._bindings["id"],
                "lid": lid,
                "sid": sid,
            },
            query=(
            ),
            headers={
            },
            body=subscriber_patch,
            form={
            },

            content_type="application/json",


            accept="application/json",

        )
        return self._transport.request(request, request_options=request_options)

class AsyncBusinessMailingSubscribersResource:
    def __init__(self, transport: AsyncTransport, bindings: Mapping[str, str] | None = None) -> None:
        self._transport = transport
        self._bindings = dict(bindings or {})

    async def from_contacts(
        self,
        *,

        lid: UUID,

        contacts_import: ContactsImport,

        request_options: RequestOptions | None = None,
    ) -> ImportResult:
        request = Request(
            operation_id="addMailingSubscribersFromContacts",
            method="POST",
            path="/api/v1/businesses/{id}/mailing/lists/{lid}/subscribers/from-contacts",
            authenticated=True,
            response=TypeAdapter(ImportResult),
            path_parameters={
                "id": self._bindings["id"],
                "lid": lid,
            },
            query=(
            ),
            headers={
            },
            body=contacts_import,
            form={
            },

            content_type="application/json",


            accept="application/json",

        )
        return await self._transport.request(request, request_options=request_options)

    async def create(
        self,
        *,

        lid: UUID,

        subscriber_input: SubscriberInput,

        request_options: RequestOptions | None = None,
    ) -> Subscriber:
        request = Request(
            operation_id="createMailingSubscriber",
            method="POST",
            path="/api/v1/businesses/{id}/mailing/lists/{lid}/subscribers",
            authenticated=True,
            response=TypeAdapter(Subscriber),
            path_parameters={
                "id": self._bindings["id"],
                "lid": lid,
            },
            query=(
            ),
            headers={
            },
            body=subscriber_input,
            form={
            },

            content_type="application/json",


            accept="application/json",

        )
        return await self._transport.request(request, request_options=request_options)

    async def export_csv(
        self,
        *,

        lid: UUID,

        request_options: RequestOptions | None = None,
    ) -> AsyncByteStream:
        request = Request(
            operation_id="exportMailingSubscribersCSV",
            method="GET",
            path="/api/v1/businesses/{id}/mailing/lists/{lid}/subscribers/export",
            authenticated=True,
            response=TypeAdapter(type(None)),
            path_parameters={
                "id": self._bindings["id"],
                "lid": lid,
            },
            query=(
            ),
            headers={
            },
            form={
            },


            accept="text/csv",

        )
        return await self._transport.stream(request, request_options=request_options)

    async def get(
        self,
        *,

        lid: UUID,

        sid: UUID,

        request_options: RequestOptions | None = None,
    ) -> Subscriber:
        request = Request(
            operation_id="getMailingSubscriber",
            method="GET",
            path="/api/v1/businesses/{id}/mailing/lists/{lid}/subscribers/{sid}",
            authenticated=True,
            response=TypeAdapter(Subscriber),
            path_parameters={
                "id": self._bindings["id"],
                "lid": lid,
                "sid": sid,
            },
            query=(
            ),
            headers={
            },
            form={
            },


            accept="application/json",

        )
        return await self._transport.request(request, request_options=request_options)

    async def import_csv(
        self,
        *,

        lid: UUID,

        consent_attested: bool,

        file: Upload,

        skip_confirmation: bool | UnsetType = UNSET,

        request_options: RequestOptions | None = None,
    ) -> ImportResult:
        request = Request(
            operation_id="importMailingSubscribersCSV",
            method="POST",
            path="/api/v1/businesses/{id}/mailing/lists/{lid}/subscribers/import",
            authenticated=True,
            response=TypeAdapter(ImportResult),
            path_parameters={
                "id": self._bindings["id"],
                "lid": lid,
            },
            query=(
            ),
            headers={
            },
            form={
                "consent_attested": consent_attested,
                "file": file,
                "skip_confirmation": skip_confirmation,
            },

            content_type="multipart/form-data",


            accept="application/json",

        )
        return await self._transport.request(request, request_options=request_options)

    async def list(
        self,
        *,

        lid: UUID,

        q: str | UnsetType = UNSET,

        status: SubscriberStatus | UnsetType = UNSET,

        tag: str | UnsetType = UNSET,

        cursor: str | UnsetType = UNSET,

        limit: int | UnsetType = UNSET,

        request_options: RequestOptions | None = None,
    ) -> SubscriberPage:
        request = Request(
            operation_id="listMailingSubscribers",
            method="GET",
            path="/api/v1/businesses/{id}/mailing/lists/{lid}/subscribers",
            authenticated=True,
            response=TypeAdapter(SubscriberPage),
            path_parameters={
                "id": self._bindings["id"],
                "lid": lid,
            },
            query=(
                QueryParameter("q", q, "form", True),
                QueryParameter("status", status, "form", True),
                QueryParameter("tag", tag, "form", True),
                QueryParameter("cursor", cursor, "form", True),
                QueryParameter("limit", limit, "form", True),
            ),
            headers={
            },
            form={
            },


            accept="application/json",

        )
        return await self._transport.request(request, request_options=request_options)

    async def iter(
        self,
        *,

        lid: UUID,

        q: str | UnsetType = UNSET,

        status: SubscriberStatus | UnsetType = UNSET,

        tag: str | UnsetType = UNSET,

        cursor: str | UnsetType = UNSET,

        limit: int | UnsetType = UNSET,

        request_options: RequestOptions | None = None,
    ) -> AsyncIterator[Subscriber]:
        seen: set[str] = set()
        next_cursor = cursor
        if isinstance(next_cursor, str) and next_cursor:
            seen.add(next_cursor)
        while True:
            page = await self.list(

                lid=lid,

                q=q,

                status=status,

                tag=tag,

                cursor=next_cursor,

                limit=limit,

                request_options=request_options,
            )
            for item in page.items:
                yield item
            next_cursor = page.next_cursor
            if isinstance(next_cursor, UnsetType) or not next_cursor:
                return
            if next_cursor in seen:
                raise PaginationError("Server returned a repeated cursor")
            seen.add(next_cursor)

    async def delete(
        self,
        *,

        lid: UUID,

        sid: UUID,

        request_options: RequestOptions | None = None,
    ) -> None:
        request = Request(
            operation_id="unsubscribeMailingSubscriber",
            method="DELETE",
            path="/api/v1/businesses/{id}/mailing/lists/{lid}/subscribers/{sid}",
            authenticated=True,
            response=TypeAdapter(type(None)),
            path_parameters={
                "id": self._bindings["id"],
                "lid": lid,
                "sid": sid,
            },
            query=(
            ),
            headers={
            },
            form={
            },


            accept="application/json",

        )
        return await self._transport.request(request, request_options=request_options)

    async def update(
        self,
        *,

        lid: UUID,

        sid: UUID,

        subscriber_patch: SubscriberPatch,

        request_options: RequestOptions | None = None,
    ) -> Subscriber:
        request = Request(
            operation_id="updateMailingSubscriber",
            method="PATCH",
            path="/api/v1/businesses/{id}/mailing/lists/{lid}/subscribers/{sid}",
            authenticated=True,
            response=TypeAdapter(Subscriber),
            path_parameters={
                "id": self._bindings["id"],
                "lid": lid,
                "sid": sid,
            },
            query=(
            ),
            headers={
            },
            body=subscriber_patch,
            form={
            },

            content_type="application/json",


            accept="application/json",

        )
        return await self._transport.request(request, request_options=request_options)

