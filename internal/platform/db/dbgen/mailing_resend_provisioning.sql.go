// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.27.0
// source: mailing_resend_provisioning.sql

package dbgen

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const markMailingResendWebhookMutation = `-- name: MarkMailingResendWebhookMutation :one
UPDATE mailing_sending_profile SET
    resend_cleanup_required = true
WHERE id = $1
  AND business_id = $2
  AND tenant_root_id = $3
  AND updated_at = $4::timestamptz
  AND mode = 'resend'
  AND resend_provisioning_token = $5::uuid
  AND resend_provisioning_expires_at > clock_timestamp()
RETURNING id
`

type MarkMailingResendWebhookMutationParams struct {
	ID                uuid.UUID `json:"id"`
	BusinessID        uuid.UUID `json:"business_id"`
	TenantRootID      uuid.UUID `json:"tenant_root_id"`
	ExpectedUpdatedAt time.Time `json:"expected_updated_at"`
	Token             uuid.UUID `json:"token"`
}

// Record cleanup intent only at the boundary that can mutate remote webhooks.
// Keep updated_at unchanged so provisioning persistence can compare the same revision.
func (q *Queries) MarkMailingResendWebhookMutation(ctx context.Context, arg MarkMailingResendWebhookMutationParams) (uuid.UUID, error) {
	row := q.db.QueryRow(ctx, markMailingResendWebhookMutation,
		arg.ID,
		arg.BusinessID,
		arg.TenantRootID,
		arg.ExpectedUpdatedAt,
		arg.Token,
	)
	var id uuid.UUID
	err := row.Scan(&id)
	return id, err
}
