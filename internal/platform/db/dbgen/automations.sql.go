// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.27.0
// source: automations.sql

package dbgen

import (
	"context"
	"time"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const countAutomationVersions = `-- name: CountAutomationVersions :one
SELECT count(*) FROM automation_version
WHERE automation_id = $1
  AND business_id = $2
  AND tenant_root_id = $3
`

type CountAutomationVersionsParams struct {
	AutomationID uuid.UUID `json:"automation_id"`
	BusinessID   uuid.UUID `json:"business_id"`
	TenantRootID uuid.UUID `json:"tenant_root_id"`
}

func (q *Queries) CountAutomationVersions(ctx context.Context, arg CountAutomationVersionsParams) (int64, error) {
	row := q.db.QueryRow(ctx, countAutomationVersions, arg.AutomationID, arg.BusinessID, arg.TenantRootID)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const getAutomation = `-- name: GetAutomation :one
SELECT id, business_id, tenant_root_id, name, description, status, allow_reenroll, active_version_id, draft_version_id, created_by_principal_id, created_at, updated_at FROM automation
WHERE id = $1 AND business_id = $2 AND tenant_root_id = $3
`

type GetAutomationParams struct {
	ID           uuid.UUID `json:"id"`
	BusinessID   uuid.UUID `json:"business_id"`
	TenantRootID uuid.UUID `json:"tenant_root_id"`
}

func (q *Queries) GetAutomation(ctx context.Context, arg GetAutomationParams) (Automation, error) {
	row := q.db.QueryRow(ctx, getAutomation, arg.ID, arg.BusinessID, arg.TenantRootID)
	var i Automation
	err := row.Scan(
		&i.ID,
		&i.BusinessID,
		&i.TenantRootID,
		&i.Name,
		&i.Description,
		&i.Status,
		&i.AllowReenroll,
		&i.ActiveVersionID,
		&i.DraftVersionID,
		&i.CreatedByPrincipalID,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getAutomationVersion = `-- name: GetAutomationVersion :one
SELECT id, business_id, tenant_root_id, automation_id, number, status, graph, trigger_kind, trigger_ref, activated_at, created_at, updated_at, content_snapshot FROM automation_version
WHERE id = $1 AND automation_id = $2
  AND business_id = $3 AND tenant_root_id = $4
`

type GetAutomationVersionParams struct {
	ID           uuid.UUID `json:"id"`
	AutomationID uuid.UUID `json:"automation_id"`
	BusinessID   uuid.UUID `json:"business_id"`
	TenantRootID uuid.UUID `json:"tenant_root_id"`
}

func (q *Queries) GetAutomationVersion(ctx context.Context, arg GetAutomationVersionParams) (AutomationVersion, error) {
	row := q.db.QueryRow(ctx, getAutomationVersion,
		arg.ID,
		arg.AutomationID,
		arg.BusinessID,
		arg.TenantRootID,
	)
	var i AutomationVersion
	err := row.Scan(
		&i.ID,
		&i.BusinessID,
		&i.TenantRootID,
		&i.AutomationID,
		&i.Number,
		&i.Status,
		&i.Graph,
		&i.TriggerKind,
		&i.TriggerRef,
		&i.ActivatedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.ContentSnapshot,
	)
	return i, err
}

const insertAutomation = `-- name: InsertAutomation :one

INSERT INTO automation (
    id, business_id, tenant_root_id, name, description, status, allow_reenroll,
    created_by_principal_id, created_at, updated_at
) VALUES (
    $1, $2, $3, $4, $5, 'draft', $6, $7, now(), now()
)
RETURNING id, business_id, tenant_root_id, name, description, status, allow_reenroll, active_version_id, draft_version_id, created_by_principal_id, created_at, updated_at
`

type InsertAutomationParams struct {
	ID                   uuid.UUID   `json:"id"`
	BusinessID           uuid.UUID   `json:"business_id"`
	TenantRootID         uuid.UUID   `json:"tenant_root_id"`
	Name                 string      `json:"name"`
	Description          *string     `json:"description"`
	AllowReenroll        bool        `json:"allow_reenroll"`
	CreatedByPrincipalID pgtype.UUID `json:"created_by_principal_id"`
}

// Spec 014 automation definition queries. Worker and trigger functions are invoked through
// raw pgx so their SECURITY DEFINER signatures remain explicit at the package boundary.
func (q *Queries) InsertAutomation(ctx context.Context, arg InsertAutomationParams) (Automation, error) {
	row := q.db.QueryRow(ctx, insertAutomation,
		arg.ID,
		arg.BusinessID,
		arg.TenantRootID,
		arg.Name,
		arg.Description,
		arg.AllowReenroll,
		arg.CreatedByPrincipalID,
	)
	var i Automation
	err := row.Scan(
		&i.ID,
		&i.BusinessID,
		&i.TenantRootID,
		&i.Name,
		&i.Description,
		&i.Status,
		&i.AllowReenroll,
		&i.ActiveVersionID,
		&i.DraftVersionID,
		&i.CreatedByPrincipalID,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const insertAutomationVersion = `-- name: InsertAutomationVersion :one
INSERT INTO automation_version (
    id, business_id, tenant_root_id, automation_id, number, status, graph,
    created_at, updated_at
) VALUES (
    $1, $2, $3, $4, $5, 'draft', $6, now(), now()
)
RETURNING id, business_id, tenant_root_id, automation_id, number, status, graph, trigger_kind, trigger_ref, activated_at, created_at, updated_at, content_snapshot
`

type InsertAutomationVersionParams struct {
	ID           uuid.UUID `json:"id"`
	BusinessID   uuid.UUID `json:"business_id"`
	TenantRootID uuid.UUID `json:"tenant_root_id"`
	AutomationID uuid.UUID `json:"automation_id"`
	Number       int32     `json:"number"`
	Graph        []byte    `json:"graph"`
}

func (q *Queries) InsertAutomationVersion(ctx context.Context, arg InsertAutomationVersionParams) (AutomationVersion, error) {
	row := q.db.QueryRow(ctx, insertAutomationVersion,
		arg.ID,
		arg.BusinessID,
		arg.TenantRootID,
		arg.AutomationID,
		arg.Number,
		arg.Graph,
	)
	var i AutomationVersion
	err := row.Scan(
		&i.ID,
		&i.BusinessID,
		&i.TenantRootID,
		&i.AutomationID,
		&i.Number,
		&i.Status,
		&i.Graph,
		&i.TriggerKind,
		&i.TriggerRef,
		&i.ActivatedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.ContentSnapshot,
	)
	return i, err
}

const listAutomationVersions = `-- name: ListAutomationVersions :many
SELECT id, business_id, tenant_root_id, automation_id, number, status,
       trigger_kind, trigger_ref, activated_at, created_at, updated_at
FROM automation_version
WHERE automation_id = $1
  AND business_id = $2
  AND tenant_root_id = $3
ORDER BY number DESC, id DESC
LIMIT LEAST(GREATEST($4::integer, 1), 101)
`

type ListAutomationVersionsParams struct {
	AutomationID uuid.UUID `json:"automation_id"`
	BusinessID   uuid.UUID `json:"business_id"`
	TenantRootID uuid.UUID `json:"tenant_root_id"`
	Lim          int32     `json:"lim"`
}

type ListAutomationVersionsRow struct {
	ID           uuid.UUID               `json:"id"`
	BusinessID   uuid.UUID               `json:"business_id"`
	TenantRootID uuid.UUID               `json:"tenant_root_id"`
	AutomationID uuid.UUID               `json:"automation_id"`
	Number       int32                   `json:"number"`
	Status       AutomationVersionStatus `json:"status"`
	TriggerKind  *string                 `json:"trigger_kind"`
	TriggerRef   *string                 `json:"trigger_ref"`
	ActivatedAt  pgtype.Timestamptz      `json:"activated_at"`
	CreatedAt    time.Time               `json:"created_at"`
	UpdatedAt    time.Time               `json:"updated_at"`
}

func (q *Queries) ListAutomationVersions(ctx context.Context, arg ListAutomationVersionsParams) ([]ListAutomationVersionsRow, error) {
	rows, err := q.db.Query(ctx, listAutomationVersions,
		arg.AutomationID,
		arg.BusinessID,
		arg.TenantRootID,
		arg.Lim,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListAutomationVersionsRow
	for rows.Next() {
		var i ListAutomationVersionsRow
		if err := rows.Scan(
			&i.ID,
			&i.BusinessID,
			&i.TenantRootID,
			&i.AutomationID,
			&i.Number,
			&i.Status,
			&i.TriggerKind,
			&i.TriggerRef,
			&i.ActivatedAt,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listAutomationVersionsAfter = `-- name: ListAutomationVersionsAfter :many
SELECT id, business_id, tenant_root_id, automation_id, number, status,
       trigger_kind, trigger_ref, activated_at, created_at, updated_at
FROM automation_version
WHERE automation_id = $1
  AND business_id = $2
  AND tenant_root_id = $3
  AND (number, id) < ($4::integer, $5::uuid)
ORDER BY number DESC, id DESC
LIMIT LEAST(GREATEST($6::integer, 1), 101)
`

type ListAutomationVersionsAfterParams struct {
	AutomationID uuid.UUID `json:"automation_id"`
	BusinessID   uuid.UUID `json:"business_id"`
	TenantRootID uuid.UUID `json:"tenant_root_id"`
	CurNumber    int32     `json:"cur_number"`
	CurID        uuid.UUID `json:"cur_id"`
	Lim          int32     `json:"lim"`
}

type ListAutomationVersionsAfterRow struct {
	ID           uuid.UUID               `json:"id"`
	BusinessID   uuid.UUID               `json:"business_id"`
	TenantRootID uuid.UUID               `json:"tenant_root_id"`
	AutomationID uuid.UUID               `json:"automation_id"`
	Number       int32                   `json:"number"`
	Status       AutomationVersionStatus `json:"status"`
	TriggerKind  *string                 `json:"trigger_kind"`
	TriggerRef   *string                 `json:"trigger_ref"`
	ActivatedAt  pgtype.Timestamptz      `json:"activated_at"`
	CreatedAt    time.Time               `json:"created_at"`
	UpdatedAt    time.Time               `json:"updated_at"`
}

func (q *Queries) ListAutomationVersionsAfter(ctx context.Context, arg ListAutomationVersionsAfterParams) ([]ListAutomationVersionsAfterRow, error) {
	rows, err := q.db.Query(ctx, listAutomationVersionsAfter,
		arg.AutomationID,
		arg.BusinessID,
		arg.TenantRootID,
		arg.CurNumber,
		arg.CurID,
		arg.Lim,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListAutomationVersionsAfterRow
	for rows.Next() {
		var i ListAutomationVersionsAfterRow
		if err := rows.Scan(
			&i.ID,
			&i.BusinessID,
			&i.TenantRootID,
			&i.AutomationID,
			&i.Number,
			&i.Status,
			&i.TriggerKind,
			&i.TriggerRef,
			&i.ActivatedAt,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listAutomations = `-- name: ListAutomations :many
SELECT id, business_id, tenant_root_id, name, description, status, allow_reenroll, active_version_id, draft_version_id, created_by_principal_id, created_at, updated_at FROM automation
WHERE business_id = $1 AND tenant_root_id = $2
ORDER BY created_at DESC, id DESC
LIMIT $3
`

type ListAutomationsParams struct {
	BusinessID   uuid.UUID `json:"business_id"`
	TenantRootID uuid.UUID `json:"tenant_root_id"`
	Limit        int32     `json:"limit"`
}

func (q *Queries) ListAutomations(ctx context.Context, arg ListAutomationsParams) ([]Automation, error) {
	rows, err := q.db.Query(ctx, listAutomations, arg.BusinessID, arg.TenantRootID, arg.Limit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Automation
	for rows.Next() {
		var i Automation
		if err := rows.Scan(
			&i.ID,
			&i.BusinessID,
			&i.TenantRootID,
			&i.Name,
			&i.Description,
			&i.Status,
			&i.AllowReenroll,
			&i.ActiveVersionID,
			&i.DraftVersionID,
			&i.CreatedByPrincipalID,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listAutomationsAfter = `-- name: ListAutomationsAfter :many
SELECT id, business_id, tenant_root_id, name, description, status, allow_reenroll, active_version_id, draft_version_id, created_by_principal_id, created_at, updated_at FROM automation
WHERE business_id = $1
  AND tenant_root_id = $2
  AND (created_at, id) < ($3::timestamptz, $4::uuid)
ORDER BY created_at DESC, id DESC
LIMIT $5
`

type ListAutomationsAfterParams struct {
	BusinessID   uuid.UUID `json:"business_id"`
	TenantRootID uuid.UUID `json:"tenant_root_id"`
	CurCreated   time.Time `json:"cur_created"`
	CurID        uuid.UUID `json:"cur_id"`
	Lim          int32     `json:"lim"`
}

func (q *Queries) ListAutomationsAfter(ctx context.Context, arg ListAutomationsAfterParams) ([]Automation, error) {
	rows, err := q.db.Query(ctx, listAutomationsAfter,
		arg.BusinessID,
		arg.TenantRootID,
		arg.CurCreated,
		arg.CurID,
		arg.Lim,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Automation
	for rows.Next() {
		var i Automation
		if err := rows.Scan(
			&i.ID,
			&i.BusinessID,
			&i.TenantRootID,
			&i.Name,
			&i.Description,
			&i.Status,
			&i.AllowReenroll,
			&i.ActiveVersionID,
			&i.DraftVersionID,
			&i.CreatedByPrincipalID,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const lockAutomationVersion = `-- name: LockAutomationVersion :one
SELECT id, business_id, tenant_root_id, automation_id, number, status, graph, trigger_kind, trigger_ref, activated_at, created_at, updated_at, content_snapshot FROM automation_version
WHERE id = $1 AND automation_id = $2
  AND business_id = $3 AND tenant_root_id = $4
FOR UPDATE
`

type LockAutomationVersionParams struct {
	ID           uuid.UUID `json:"id"`
	AutomationID uuid.UUID `json:"automation_id"`
	BusinessID   uuid.UUID `json:"business_id"`
	TenantRootID uuid.UUID `json:"tenant_root_id"`
}

func (q *Queries) LockAutomationVersion(ctx context.Context, arg LockAutomationVersionParams) (AutomationVersion, error) {
	row := q.db.QueryRow(ctx, lockAutomationVersion,
		arg.ID,
		arg.AutomationID,
		arg.BusinessID,
		arg.TenantRootID,
	)
	var i AutomationVersion
	err := row.Scan(
		&i.ID,
		&i.BusinessID,
		&i.TenantRootID,
		&i.AutomationID,
		&i.Number,
		&i.Status,
		&i.Graph,
		&i.TriggerKind,
		&i.TriggerRef,
		&i.ActivatedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.ContentSnapshot,
	)
	return i, err
}

const pruneAutomationVersions = `-- name: PruneAutomationVersions :execrows
WITH total AS (
    SELECT count(*)::integer AS value
    FROM automation_version current_version
    WHERE current_version.automation_id = $1
      AND current_version.business_id = $2
      AND current_version.tenant_root_id = $3
), removable AS (
    SELECT v.id
    FROM automation_version v
    WHERE v.automation_id = $1
      AND v.business_id = $2
      AND v.tenant_root_id = $3
      AND v.status = 'superseded'
      AND NOT EXISTS (
          SELECT 1 FROM automation_enrollment e WHERE e.version_id = v.id
      )
    ORDER BY v.number, v.id
    LIMIT GREATEST((SELECT value FROM total) - $4::integer, 0)
)
DELETE FROM automation_version v
USING removable r
WHERE v.id = r.id
`

type PruneAutomationVersionsParams struct {
	AutomationID uuid.UUID `json:"automation_id"`
	BusinessID   uuid.UUID `json:"business_id"`
	TenantRootID uuid.UUID `json:"tenant_root_id"`
	TargetCount  int32     `json:"target_count"`
}

func (q *Queries) PruneAutomationVersions(ctx context.Context, arg PruneAutomationVersionsParams) (int64, error) {
	result, err := q.db.Exec(ctx, pruneAutomationVersions,
		arg.AutomationID,
		arg.BusinessID,
		arg.TenantRootID,
		arg.TargetCount,
	)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const updateAutomationDefinition = `-- name: UpdateAutomationDefinition :one
UPDATE automation SET
    name = $1,
    description = $2,
    allow_reenroll = $3,
    updated_at = now()
WHERE id = $4
  AND business_id = $5
  AND tenant_root_id = $6
  AND status <> 'archived'
RETURNING id, business_id, tenant_root_id, name, description, status, allow_reenroll, active_version_id, draft_version_id, created_by_principal_id, created_at, updated_at
`

type UpdateAutomationDefinitionParams struct {
	Name          string    `json:"name"`
	Description   *string   `json:"description"`
	AllowReenroll bool      `json:"allow_reenroll"`
	ID            uuid.UUID `json:"id"`
	BusinessID    uuid.UUID `json:"business_id"`
	TenantRootID  uuid.UUID `json:"tenant_root_id"`
}

func (q *Queries) UpdateAutomationDefinition(ctx context.Context, arg UpdateAutomationDefinitionParams) (Automation, error) {
	row := q.db.QueryRow(ctx, updateAutomationDefinition,
		arg.Name,
		arg.Description,
		arg.AllowReenroll,
		arg.ID,
		arg.BusinessID,
		arg.TenantRootID,
	)
	var i Automation
	err := row.Scan(
		&i.ID,
		&i.BusinessID,
		&i.TenantRootID,
		&i.Name,
		&i.Description,
		&i.Status,
		&i.AllowReenroll,
		&i.ActiveVersionID,
		&i.DraftVersionID,
		&i.CreatedByPrincipalID,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const updateAutomationVersionGraph = `-- name: UpdateAutomationVersionGraph :one
UPDATE automation_version SET graph = $1, updated_at = now()
WHERE id = $2
  AND automation_id = $3
  AND business_id = $4
  AND tenant_root_id = $5
  AND status = 'draft'
RETURNING id, business_id, tenant_root_id, automation_id, number, status, graph, trigger_kind, trigger_ref, activated_at, created_at, updated_at, content_snapshot
`

type UpdateAutomationVersionGraphParams struct {
	Graph        []byte    `json:"graph"`
	ID           uuid.UUID `json:"id"`
	AutomationID uuid.UUID `json:"automation_id"`
	BusinessID   uuid.UUID `json:"business_id"`
	TenantRootID uuid.UUID `json:"tenant_root_id"`
}

func (q *Queries) UpdateAutomationVersionGraph(ctx context.Context, arg UpdateAutomationVersionGraphParams) (AutomationVersion, error) {
	row := q.db.QueryRow(ctx, updateAutomationVersionGraph,
		arg.Graph,
		arg.ID,
		arg.AutomationID,
		arg.BusinessID,
		arg.TenantRootID,
	)
	var i AutomationVersion
	err := row.Scan(
		&i.ID,
		&i.BusinessID,
		&i.TenantRootID,
		&i.AutomationID,
		&i.Number,
		&i.Status,
		&i.Graph,
		&i.TriggerKind,
		&i.TriggerRef,
		&i.ActivatedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.ContentSnapshot,
	)
	return i, err
}
