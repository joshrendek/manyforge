[project]
name = "manyforge-sdk-tooling"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = [
  "PyYAML==6.0.2",
  "openapi-spec-validator==0.7.2",
  "build==1.3.0",
  "hatchling==1.27.0",
]

[tool.uv]
package = false
