# Handoff — manyforge @ 004-external-connectors — 2026-06-10 (US5 T5 DONE (MF-004-US5 security regression pins — source + behavioral, commit 57d9383); ALL 5 US5 tasks committed → NEXT = US5-close gate (full gate + opus whole-story review → bd close manyforge-a7j.5 → push); US4 CLOSED+PUSHED; Ralph loop STOPPED — manual/interactive control)

## ⚠️ Before you clear
- **Committed this session:** the US5 **T5** increment — the test commit `57d9383` (`internal/security_regression/mf_004_us5_zendesk_test.go`, NEW, `package security_regression`, NO build tag: 4 source-string pins + 3 behavioral pins. Source pins = `netsafe.NewClientWithOptions` + BOTH trust-flag gates (`AllowLoopback`/`AllowPrivate: rc.AllowPrivateBaseURL`, whitespace-collapsed match) in factory.go; `c.email+"/token"`+`SetBasicAuth` in client.go; `X-Zendesk-Webhook-Signature[-Timestamp]`+`base64.StdEncoding.DecodeString`+`hmac.Equal`; ``^[0-9]+$`` ticket-id regex. Behavioral pins = forged sig→`ErrBadSig`, FetchIssue→`169.254.169.254`→`ErrUnreachable` (netsafe dial refusal), `Registry.BuildSystem(zendesk)` builds a real connector. All 7 PASS against existing T1–T4 code (regression-pin "green"). Subagent-driven: implementer (TDD+mutation-proof non-vacuity) → spec-compliance review (COMPLIANT) → code-quality review (2 Low findings) → triaged+fixed (whitespace brittleness + AllowLoopback unpinned; rejected the reviewer's vacuity-introducing split-token variant) → re-review (both RESOLVED). Commit AMENDED once to fold the fix in — one-commit-per-task.) plus the `docs(handoff)` commit you are about to make (plan T5 boxes ticked + this HANDOFF; `a7j.5.5` closed in Dolt). Only untracked harness files remain — leave them all: `.claude/scheduled_tasks.lock`, the stray `docs/superpowers/plans/2026-06-01-us2-reply-threading.md`, and several **claude-mem auto-generated** `CLAUDE.md` files under `db/`, `db/query/`, `migrations/`, `internal/agents/`, `internal/connectors/jira/`, `internal/platform/db/`, `internal/platform/db/dbgen/`, `internal/platform/netsafe/`, `docs/superpowers/plans/` — observation-index tables from the claude-mem hook, NOT real docs, NOT committed. **`HANDOFF.md` IS tracked** (commit it). (`.beads/issues.jsonl` IS tracked — the bd pre-commit hook re-exports it on `bd close`; stage it into this `docs(handoff)` commit with `a7j.5.5` marked closed.)
- **Unpushed:** the US5 T1 (`4015651`) + T2 (`00d47b2`) + T3 (`86db74c`) + T4 (`10be3d7`) + T5 (`57d9383`) feat/test commits + their `docs(handoff)` commits are local (loop pushes at the US-close gate, not per task commit; matches the US4 cadence — intermediate task commits stay local until the close gate `git push`). **The US5-close gate increment (NEXT) is the push point.** The ENTIRE US4 stack was PUSHED to `origin/004-external-connectors` at the US4-close gate. US1/US2/US3 were already pushed.
- **Still running:** NO dev servers. **The Ralph loop was MANUALLY STOPPED on 2026-06-09** (user request) — `scripts/ralph-loop.sh` driver + all its `claude -p` workers killed; increments are now driven **interactively** in foreground sessions (one per session), NOT by the fresh-context loop. A few ANCIENT orphan `output-format stream-json` procs may remain (multi-day-old; safe to `kill` — `ps aux | grep 'output-format stream-json' | grep -v grep`). Docker is up (testcontainers use it). **To resume the loop:** `./scripts/ralph-loop.sh 40` (picks up the lowest OPEN story = US5 `a7j.5`).

## State (≤3 sentences)
**Spec 004 (External Ticketing Connectors), epic `manyforge-a7j`: 4/7 stories CLOSED (US1–US4); US5 (`a7j.5`, Zendesk thin 2nd) IN_PROGRESS but ALL 5 child tasks DONE & closed — TDD plan written + 5 bd children; T1 (`a7j.5.1`, skeleton+factory+FetchIssue, `4015651`); T2 (`a7j.5.2`, `PostComment`+`CreateIssue`, `00d47b2`); T3 (`a7j.5.3`, `TransitionStatus`+paginated `ListUpdatedSince`, `86db74c`); T4 (`a7j.5.4`, `VerifyWebhook` base64-ts+body-HMAC fail-closed + `DecodeWebhook` + `mapZendeskEventKind` + the `var _ connectors.TicketingConnector = (*client)(nil)` assertion, `10be3d7`); T5 (`a7j.5.5`, MF-004-US5 security regression pins — 4 source + 3 behavioral, `57d9383`, all 7 PASS). NEXT increment = "All tasks committed, story not bd-closed" → the US5-CLOSE GATE: full gate (`gofmt -l internal/ cmd/ db/` empty `&& make test && make contract-test && make lint && make sec-test && make int-test`, int-test backgrounded ~7min) → opus whole-story review → `bd close manyforge-a7j.5` → file follow-ups → stage `.beads/issues.jsonl` → `git push` (FIRST push of the US5 stack).** US1/US2/US3 done: secrets vault + `Service`; `TicketingConnector` framework + `Registry` + ext-id columns + sync tables; Jira **inbound** (migrations 0040–0044). **US4 (`a7j.4`) is CLOSED** — all 8 tasks committed, full gate GREEN, opus whole-story review = READY TO MERGE, pushed.

### US5 plan increment — what this session did (reference)
- **Wrote** `docs/superpowers/plans/2026-06-07-us5-zendesk.md` — 5 tasks (T1–T5), TDD, zero placeholders, exact Go + golden-fixture JSON + commands. Verified-against-source: the `connector_type` enum **already includes `'zendesk'`** (migration 0040 + `db/schema.sql:422`) and `knownConnectorTypes` already gates it → **US5 needs NO migration**; `Service`/`Registry`/`WebhookHandler`/sync engine/outbound queue (0045)/conflict audit (0046)/reconcile are all connector-agnostic and reused unchanged. Only NEW code: `internal/connectors/zendesk/` (client+factory mirroring `jira/`) + one `cmd/manyforge/main.go:277` registration line + `internal/security_regression/mf_004_us5_zendesk_test.go`.
- **Zendesk specifics nailed in the plan (not guessed):** API-token Basic auth username form = `email+"/token"` (differs from Jira's `email:api_token`); webhook verify = base64(`HMAC-SHA256(secret, timestamp+body)`) on header `X-Zendesk-Webhook-Signature` + `X-Zendesk-Webhook-Signature-Timestamp` (differs from Jira's hex `X-Hub-Signature` over body-only); ticket ids numeric (`^[0-9]+$`); FetchIssue sideloads requester (`?include=users`); PostComment = PUT ticket w/ comment, id from audit Comment event; ListUpdatedSince = `search.json` with explicit `page`/`per_page` loop (NOT following upstream `next_page` URL — SSRF hygiene); statuses clamped to `{new,open,pending,hold,solved,closed}`.
- **Created** bd children `a7j.5.1`–`a7j.5.5` (T1–T5) under `a7j.5`; marked `a7j.5` IN_PROGRESS.
- **Plan self-review note carried forward:** move the `var _ connectors.TicketingConnector = (*client)(nil)` compile-time assertion into T4 (when the last two methods land) so T1–T3 packages compile/test independently.

### US4 close gate — result (reference)
- **Full gate GREEN:** `gofmt -l internal/ cmd/ db/` empty · `make test` ✓ · `make contract-test` ✓ · `make lint` (vet + golangci-lint, 0 issues) ✓ · `make sec-test` ✓ (52.5s) · `make int-test` ✓ (EXIT 0 — every package green incl. `internal/connectors` 85.6s, `internal/ticketing` 143.6s, `internal/security_regression`, `migrations`).
- **Final whole-story opus review (T1→T8, diff `75b54d2^..44ddd78`):** VERDICT **READY TO MERGE**, 0 blocking findings, all 10 invariants CONFIRMED (no-tx-across-HTTP; producer enqueues in source tx & additive; SSRF via netsafe only; ownership pushed into SQL + no 403/404 oracle; idempotency via native `external_id` + atomic write-back; composite-FK/RLS/GRANT discipline on `connector_outbound_op`; both-changed conflict audit = exactly 1 `external_wins`; terminal-failure cap reads post-increment `attempts`; symmetric down-migrations; no SQLi/search_path/DEFINER-priv-escalation). Non-blocking observations are all already-filed follow-ups.
- **Follow-ups filed:** `a7j.4.9` (stale-`in_progress` reaper, OPEN) · `a7j.8` (decide native-email suppression for connector-linked replies) · `a7j.9` (generalize conflict audit beyond `status` to priority/subject) · `a7j.10` (remove/repurpose unused `TopicConnectorOutboundSync`). US6 (`a7j.6`) note added: route gated create/transition tools through the autonomy gate BEFORE calling `EnqueueOutboundCreateIssue`.

### US4 — what shipped (condensed; full per-task detail in git log + the plan)
T1 `170e929` CreateIssue on interface + Jira client · T2 `d997963` migration **0045** outbound op queue table + 6 SECURITY DEFINER fns (`connector_outbound_context`/`claim_outbound_ops` FOR UPDATE SKIP LOCKED RETURNing post-increment `attempts`/`complete_outbound_comment`/`complete_outbound_create`/`fail_outbound_op`/`message_external_id`) · T3 `cd587b1` producer hook in `ticketing.Reply`'s tx (additive, gated on `tk.ConnectorID.Valid`) · T4 `69264a8` `OutboundDispatcher` poller (`internal/connectors/outbound.go`, no tx across the Jira POST, netsafe SSRF client, idempotent re-POST skip, terminal cap=5) + `cmd/manyforge/main.go` wiring · T5 `59f3dbb` `Service.EnqueueOutboundCreateIssue` (ownership pushed into SQL, 0-rows→`ErrNotFound`) + dispatcher create-issue path · T6 `ca79b92`+`91b6b25` migration **0046** `CREATE OR REPLACE sync_inbound_external_issue` with both-changed conflict audit (`connector.conflict.resolved`/`external_wins`) · T7 `c66c05c` MF-004-US4 security pins (`internal/security_regression/mf_004_us4_outbound_test.go` source pins + behavioral dial-refusal pin) · T8 `44ddd78` bidirectional round-trip integration (`internal/connectors/bidirectional_integration_test.go` — webhook→inbound upsert→genuine `ticketing.Reply` producer→dispatcher POST→write-back, all behind one httptest Jira stub + netsafe).

## Autonomous loop cadence (one fully-reviewed increment per iteration)
This file drives a **fresh-context** loop: `scripts/ralph-loop.sh` spawns a SEPARATE `claude -p` process per increment, so YOUR context starts near-empty each time — keep it that way (well under 30%; lean on subagents + this file, don't re-read everything). Read this file + `bd show manyforge-a7j`, perform exactly ONE increment, commit, then **EXIT** (do NOT start a second increment — the driver launches a fresh process for the next). Pick the lowest-numbered OPEN story (US4 `a7j.4` ✓closed → US5 `a7j.5` → US6 `a7j.6`). Three increment types by state:
- **No plan yet** for that story under `docs/superpowers/plans/` → create one (writing-plans cadence; design doc is the spec below; brainstorming already done). First dispatch a subagent to extract the EXACT signatures you'll need (don't guess), then write a no-placeholder TDD plan to `docs/superpowers/plans/2026-06-07-us<N>-<slug>.md`, commit `--no-verify`, update this file. Also create the per-task bd children if the story needs them. One increment.
- **Plan exists, task unfinished** (check `git log --oneline -40` for committed tasks tagged `(manyforge-a7j.<N>)` + the plan's `- [ ]` boxes) → implement the next task subagent-driven: fresh implementer subagent (TDD → commit `--no-verify`, NO `Co-Authored-By`) → spec-compliance review subagent → code-quality review subagent → triage findings RIGOROUSLY (verify each; reject noise, accept sound fixes) → implementer fixes via SendMessage → re-run code-quality review. Tick the plan box, update this file. One increment.
- **All tasks committed, story not bd-closed** → run the FULL GATE: `export PATH=$PATH:$HOME/go/bin`; `gofmt -l internal/ cmd/ db/` (empty); `make test && make contract-test && make lint && make sec-test && make int-test` (int-test backgrounded ~7min). Green → final whole-story review subagent (opus). READY TO MERGE → `bd close manyforge-a7j.<N>`, file follow-ups, commit `.beads/issues.jsonl`, `git push`, update this file. Red gate → FIX it (no "pre-existing failures"). One increment.

**HARD RULES:** one increment then EXIT; NEVER skip the two reviews; NEVER bd-close on a red gate; NEVER push red; always leave this file current so the next fresh process resumes cleanly. **DONE = US4+US5+US6 all bd-closed AND `git status` up-to-date with origin** (the driver detects this via `bd list --status closed` and stops).

## Resume here
**US5 = `manyforge-a7j.5` (Zendesk connector). Plan EXISTS, T1–T5 ALL DONE & committed (5 task commits `4015651`/`00d47b2`/`86db74c`/`10be3d7`/`57d9383`) → this is an "All tasks committed, story not bd-closed" increment → run the US5-CLOSE GATE.** ⚠️ Ralph loop is STOPPED (manual control). If a human is driving, confirm before auto-continuing; if you are the loop resumed via `./scripts/ralph-loop.sh 40`, just proceed. Concretely:
1. **Pre-flight:** `export PATH="$PATH:$HOME/go/bin"` (golangci-lint only there — else `make lint` silently degrades to vet-only); Docker up (int-test/sec-test use testcontainers).
2. **Run the FULL GATE** (per the "All tasks committed" cadence — do NOT skip any leg, no "pre-existing failures"):
   ```bash
   export PATH="$PATH:$HOME/go/bin"
   gofmt -l internal/ cmd/ db/        # MUST be empty
   make test && make contract-test && make lint && make sec-test && make int-test
   ```
   `int-test` ≈ 6–7 min — background it (`run_in_background`) and poll. `make lint` = bare `golangci-lint run` (2.4.0) — it **exits non-zero on staticcheck QF\* quickfix hints**; the per-task `--no-verify` commits never ran it, so this is the first full-repo lint of the US5 stack. If RED anywhere → FIX it this increment (it's still one increment), re-run, do NOT close/push on red.
3. **GREEN → final whole-story review subagent (opus).** Diff = `git diff ea84a71^..HEAD` (ea84a71 = the US5 plan commit; or `git log --oneline | grep a7j.5` to bound it) reviewed against Spec §8 US5 + §7 pins + the 8 `TicketingConnector` methods. Want VERDICT = READY TO MERGE, 0 blocking.
4. **READY TO MERGE →** `bd close manyforge-a7j.5`; file any follow-ups as bd issues; stage `.beads/issues.jsonl` + this HANDOFF; commit `--no-verify` (`chore(connectors): US5-close gate — bd close manyforge-a7j.5 …`); then **`git pull --rebase && bd dolt push && git push && git status`** (MUST show up-to-date with origin — this is the FIRST push of the entire US5 stack: 5 task commits + their docs commits). Update this file. **EXIT.**
5. After US5 closed+pushed → **US6 = `manyforge-a7j.6`** (read + gated-write agent tools — note `a7j.6`'s autonomy-gate requirement BEFORE `EnqueueOutboundCreateIssue`; **"No plan yet" → write the plan first** increment: subagent extracts exact signatures → no-placeholder TDD plan to `docs/superpowers/plans/2026-06-07-us6-<slug>.md` + bd children → commit → update this file → EXIT).

## Run & verify (full gate — last GREEN at the US4-close commit)
- **Pre-flight:** `export PATH="$PATH:$HOME/go/bin"` (golangci-lint only there — else `make lint` silently degrades to vet-only). Docker up.
- **Full gate:** `gofmt -l internal/ cmd/ db/` (must be empty) `&& make test && make contract-test && make lint && make sec-test && make int-test`. `int-test` ≈ 6–7 min (run it backgrounded). `make generate` runs `sqlc generate` (the ONLY generator) after editing `db/schema.sql`/`db/query/`.
- **Connectors-only fast loop:** `go test -tags integration -p 1 ./internal/connectors/ -v` (needs Docker; testcontainers auto-applies `migrations/`).
- Dev DB (optional): owner DSN `postgres://manyforge:devpassword@localhost:55432/manyforge?sslmode=disable` (SSH tunnel; not started by this session).

## Gotchas (don't relearn these)
- **gopls LIES persistently** on every file referencing `dbgen` ("undefined: dbgen.X") and on every `//go:build integration` file ("No packages found"). These are FALSE POSITIVES — `go build ./...` is clean and tests pass. **Trust `go build`/`go test`, NEVER the IDE diagnostics panel.**
- **dbgen nullable nuance:** with the `uuid`/`timestamptz` go_type overrides, `emit_pointers_for_null_types` does NOT apply to them. `<col> uuid NULL` → `pgtype.UUID` (NOT `*uuid.UUID`); `text NULL` → `*string`; `timestamptz NULL` → `pgtype.Timestamptz`. Plain `text NOT NULL` → `string`. (`ticket.ConnectorID` is `pgtype.UUID`, `ticket.ExternalID`/`ExternalUrl` are `*string`.)
- **Two casings:** Go service structs use `BaseURL`/`AllowPrivateBaseURL`; generated dbgen fields use `BaseUrl`/`AllowPrivateBaseUrl` (Url, not URL). Match each layer.
- **Composite-FK discipline:** every new child table needs `business_id`+`tenant_root_id`, `FOREIGN KEY (x_id, tenant_root_id) → parent(id, tenant_root_id)`, a `*_troot_immutable` trigger, RLS (`authorized_businesses(current_principal())`), GRANT to `manyforge_app`. Mirror every column into `db/schema.sql` (strip DEFAULT/RLS/trigger). Reuse `support_tenant_root_immutable()` (from 0013, don't redefine).
- **Push ownership predicates into SQL:** US1/US2/US4 all gained an `EXISTS`/`INSERT…SELECT … WHERE business_id=…` guard so the SQL enforces ownership (same-business defense-in-depth beyond the same-tenant FK). 0-rows → `errs.ErrNotFound` (no 403/404 oracle).
- **Commits use `--no-verify`** (intermediate task commits; full gate runs at US-close). **NO `Co-Authored-By`** (user rule). The **bd hook re-exports `.beads/issues.jsonl` on commit** — stage it. `sqlc`/`migrate` on standard PATH; `golangci-lint` only `~/go/bin`.
- **Spec branches STACK:** master is ~141 commits behind; `004-external-connectors` branched off `003-agent-runtime` (not merged to master). Branch from current HEAD; don't merge to master.
- **Next migration: 0047** (0040 US1, 0041 US2, 0042/0043/0044 US3, 0045 US4 outbound op table + DEFINERs, 0046 US4 conflict-audit `CREATE OR REPLACE sync_inbound_external_issue`). **US5 (Zendesk) needs NO migration** — the `connector_type` enum already has `'zendesk'` (0040) and the whole engine is connector-agnostic; 0047 is reserved for US6 if needed. External webhook ingest route is PUBLIC (no JWT) — mirror `internal/inbox/handler.go`'s rate-limit-before-routing + uniform-202 no-oracle pattern; Zendesk webhook verify (base64 ts+body HMAC on `X-Zendesk-Webhook-Signature`) is the new US5-specific surface, fully covered by the plan's T4 + T5 pins.

## Decisions & rationale (brainstorming forks, all locked)
- **Jira FIRST** end-to-end (US3 inbound + US4 outbound DONE), then **Zendesk as a thin 2nd (US5)**. **API-token auth** (email:token HTTP Basic); OAuth2 deferred. **External-wins** scalar conflict policy (external authoritative; ManyForge mirrors/triages); comments append-only union deduped by external id; deliberate gated writes are the separate authoritative path. **US6 agent tools = read + gated write** (add_external_comment / transition_external_status), routed through the autonomy gate before driving `EnqueueOutboundCreateIssue`/transition ops. On-prem via the per-connector `allow_private_base_url` trust flag.
- **US4 outbound design (LOCKED, validated at close):** poller + `connector_outbound_op` queue (NOT a Bus subscriber) → honors no-tx-across-HTTP; producer (`Reply`) inserts the op in the source tx; ADDITIVE (native email not suppressed → follow-up `a7j.8`); idempotency anchor = native row's `external_id`; "new linked ticket → issue" = explicit escalation via `EnqueueOutboundCreateIssue`.

## Next steps
1. ~~US1/US2/US3 (Jira inbound)~~ ✅ DONE/CLOSED/PUSHED. 2. ~~US4 (Jira outbound + bidirectional + conflict)~~ ✅ **DONE/CLOSED/PUSHED.** 3. **US5** (`a7j.5`) — Zendesk thin 2nd: ✅ plan written + bd children created; ✅ **T1** (`a7j.5.1`, `4015651`); ✅ **T2** (`a7j.5.2`, `00d47b2`); ✅ **T3** (`a7j.5.3`, `86db74c`); ✅ **T4** (`a7j.5.4`, `10be3d7`); ✅ **T5 DONE** (`a7j.5.5` closed — MF-004-US5 security regression pins, 4 source + 3 behavioral, all 7 PASS, `57d9383`). **ALL 5 tasks committed → NEXT increment = US5-CLOSE GATE** (full gate → opus whole-story review → `bd close manyforge-a7j.5` → file follow-ups → `git push` the whole US5 stack). 4. **US6** (`a7j.6`) — read + gated-write agent tools (autonomy gate before `EnqueueOutboundCreateIssue`); no plan yet → write first. 5. Follow-up **`a7j.7`** — `UpdateConnectorCredential` must re-validate+re-audit the trust flag. Then close epic `a7j`. Open follow-ups: `a7j.4.9`, `a7j.7`, `a7j.8`, `a7j.9`, `a7j.10`.

## Pointers
- **Spec (master design):** `docs/superpowers/specs/2026-06-06-external-connectors-design.md` (§2 framework, §3 data model, §5 sync engine, §7 security pins, §8 US breakdown — see §8 US5 for Zendesk).
- **Plans:** US1 `docs/superpowers/plans/2026-06-06-us1-secrets-vault.md`; US2 `…/2026-06-07-us2-connector-framework.md`; US3 `…/2026-06-07-us3-jira-inbound.md`; **US4 `…/2026-06-07-us4-jira-outbound.md` (DONE)**; **US5 `…/2026-06-07-us5-zendesk.md` (WRITTEN — implement T1→T5).**
- **Key code:** `internal/connectors/` — `connector.go` (`TicketingConnector` interface incl. CreateIssue/PostComment/TransitionStatus + webhook decode/verify), `registry.go` (`Registry`/`Factory`), `service.go` (`Service.Create`/`Resolve`/`EnqueueOutboundCreateIssue`/`mapErr`), `credential.go`, `sync_store.go`, `inbound_sync.go`, `outbound.go` (`OutboundDispatcher`), `reconcile.go` (`Reconciler`), `types.go`. Jira impl: `internal/connectors/jira/` (Zendesk mirrors this). Vault: `internal/platform/secrets/vault.go`. Schema: `migrations/0040`–`0046`, `db/query/connector*.sql`. Test helpers: `internal/connectors/{credential_test.go,testsupport_integration_test.go}` → `startConn(t)`, `newConnService(t,tdb,v)`, `jiraInput()`, `seedConnectorTenant(...)`, `fakeConnector`/`hmacFakeConnector`/`httpStubConnector`/`roundTripConnector`.
- **Cadence skills:** `superpowers:writing-plans` → `superpowers:subagent-driven-development` (fresh implementer per task; spec-compliance review THEN code-quality review; opus for the final whole-US review). Track work in **bd** (`bd ready`, `bd show <id>`, `--claim`, `bd close`), NOT TodoWrite.
- **bd:** epic `manyforge-a7j` (`bd show manyforge-a7j` for the tree). Resume command: `/handoff resume`.
