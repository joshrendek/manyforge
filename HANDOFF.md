# Handoff — manyforge @ 004-external-connectors — 2026-06-09 (T7 DONE; Ralph loop STOPPED — manual/interactive control)

## ⚠️ Before you clear
- **Uncommitted:** NONE (only untracked harness files — leave them all: `HANDOFF.md`, `.claude/scheduled_tasks.lock`, the stray `docs/superpowers/plans/2026-06-01-us2-reply-threading.md`, and several **claude-mem auto-generated** `CLAUDE.md` files under `db/`, `db/query/`, `migrations/`, `internal/agents/`, `internal/platform/db/dbgen/`, `internal/platform/netsafe/` — observation-index tables from the claude-mem hook, NOT real docs, NOT committed).
- **Unpushed:** US1+US2+US3 pushed to `origin/004-external-connectors`. **~16 local commits unpushed** (loop driver `bff9058` → US4 plan `75b54d2` → T1 `170e929` → … → T5 `59f3dbb` → T5-handoff `d62658c` → T6 `ca79b92` + T6-review-fixes `91b6b25` → T6-handoff `9a59ace` → **T7 `c66c05c`**, plus this T7-handoff/bookkeeping commit) — per cadence they push at the US4-close gate (after T8), NOT per-task. Nothing pushed yet.
- **Still running:** NO dev servers. **The Ralph loop was MANUALLY STOPPED on 2026-06-09** (user request) — `scripts/ralph-loop.sh` driver + all its `claude -p` workers killed; US4 tasks are now being driven **interactively** in foreground sessions (one increment per session), NOT by the fresh-context loop. **~6 ANCIENT orphan `output-format stream-json` procs remain** (multi-day-old, from prior sessions — left intentionally; safe to `kill` if you want: `ps aux | grep 'output-format stream-json' | grep -v grep`). Docker is up (testcontainers use it). **To resume the loop:** `./scripts/ralph-loop.sh 40` (it will pick up the lowest OPEN US4 child = T8).

## State (≤3 sentences)
**Spec 004 (External Ticketing Connectors), epic `manyforge-a7j`: 3/7 complete; US4 in progress (7/8 tasks done — only T8 remains).** **US1/US2/US3** (`a7j.1/.2/.3`) are DONE/CLOSED/PUSHED — secrets vault + `Service`; `TicketingConnector` framework + `Registry` + ext-id columns + sync tables; Jira **inbound** (client + signed webhook + external-wins upsert + reconcile poll, migrations 0040–0044). **US4 (`a7j.4`, Jira outbound + bidirectional + conflict): plan committed** at `docs/superpowers/plans/2026-06-07-us4-jira-outbound.md` (**8 tasks**, 8 bd children). **US4 T1 DONE** (`170e929`, `a7j.4.1` CLOSED): `ExternalIssueDraft` + `CreateIssue` on the `TicketingConnector` interface + Jira `client.CreateIssue`. **US4 T2 DONE** (`d997963`, `a7j.4.2` CLOSED): **migration 0045** — `connector_outbound_op` queue table (composite FK + tenant-root trigger + RLS + GRANT) + 5 SECURITY DEFINER fns (`connector_outbound_context`, `claim_outbound_ops` w/ FOR UPDATE SKIP LOCKED, `complete_outbound_comment`, `complete_outbound_create`, `fail_outbound_op`) + `db/schema.sql` mirror + white-box test `TestOutboundOpClaimComplete`. **US4 T3 DONE** (commit `cd587b1`, `a7j.4.3` CLOSED): producer hook — new `db/query/connector_outbound.sql` (`EnqueueOutboundComment` + `EnqueueOutboundCreate`, ownership/linkage predicate pushed into SQL, connector_id/tenant_root_id derived from the ticket row) + sqlc-generated `dbgen` + hook in `ticketing.Reply`'s `WithPrincipal` tx (after `BumpTicketActivity`, before audit, gated on `tk.ConnectorID.Valid`, ADDITIVE — email NOT suppressed) + `//go:build integration` test `TestReplyEnqueuesOutboundOpForConnectorLinkedTicket` (RED→GREEN, linked→1 `comment` op w/ body+message_id, plain→0, `ticket.replied` count=2 proves additivity); spec-compliance ✅ + code-quality APPROVED (0 Critical/Important; 1 Minor — `EnqueueOutboundCreate` ahead of its T5 consumer — REJECTED as intentional per plan). **US4 T4 DONE** (commit `69264a8`, `a7j.4.4` CLOSED): `internal/connectors/outbound.go` — `OutboundDispatcher` poller mirroring `Reconciler` (claims pending ops via `claim_outbound_ops`, no DB tx across the HTTP POST, posts comment via `Registry.BuildSystem` behind the netsafe SSRF client, writes `external_id` back via `complete_outbound_comment`; idempotent re-POST skip via a new `message_external_id` DEFINER; terminal-failure cap reads the REAL post-increment `attempts` — **migration 0045's `claim_outbound_ops` was extended in-place to RETURN `attempts`** since the T2 sketch hardcoded `0`, which would loop a permanently-failing op forever) + `cmd/manyforge/main.go` wiring (construct + `go Run` alongside Reconciler) + integration tests `TestOutboundDispatcherPostsComment` (POST→write-back→done, 2nd pass no re-POST) and `TestOutboundDispatcherTerminalFailureCap` (500 stub → 5 passes → `status='failed'`, `attempts=5`); spec-compliance ✅ + code-quality APPROVED after one fix round (reviewer ran the integration suite green; 4 fixes applied & amended: misleading `recordFailure` comment corrected, dup-window documented, config-unmarshal error no longer swallowed, terminal-cap test added; reaper for stale-`in_progress` ops deferred to filed follow-up `a7j.4.9`). **US4 T5 DONE** (commit `59f3dbba`, `a7j.4.5` CLOSED): `Service.EnqueueOutboundCreateIssue(ctx, principalID, businessID, ticketID, connectorID)` in `internal/connectors/service.go` — escalation method that verifies connector ownership via `GetConnector{ID,BusinessID}` then enqueues a `create_issue` op with the **ownership predicate pushed into SQL** (INSERT…SELECT matches only a ticket owned by `businessID` AND `connector_id IS NULL`); 0-rows → `errs.ErrNotFound` (no oracle, no 403/404 split — foreign-connector and foreign/unknown/already-linked-ticket both funnel to ErrNotFound). `EnqueueOutboundCreate` query flipped `:exec`→`:execrows` + `make generate` so it returns the affected-row count (dbgen `EnqueueOutboundCreate(ctx,params) (int64, error)`; `Body` is `string` not `*string` → pass `""`). NO new dispatcher code — T4's `dispatchCreate` create-branch is reused & now has its first end-to-end coverage. Tests in `outbound_integration_test.go`: `TestOutboundDispatcherCreatesIssue` (escalate UNLINKED ticket → `dispatchOnce` → native row linked: `connector_id`+`external_id`="SUP-99"+`external_url` ends `/browse/SUP-99`) and `TestEnqueueOutboundCreateIssueOwnership` (unknown ticket → ErrNotFound); new `seedOutboundCreate` helper (connector `config='{"project_key":"SUP","issue_type":"Task"}'` + UNLINKED ticket). spec-compliance ✅ (reviewer ran both tests green, verified no-oracle funnel) + code-quality "ready to merge: Yes" (0 Critical/Important; 3 Minor ALL applied & amended into `59f3dbba`: comment "owned+enabled"→"owned, enabled enforced downstream"; `external_url` now asserted (was a dead read); duplicated ~25-line stub-jira SSRF factory extracted into shared `registerStubJira(svc)` called by both `seedOutboundConnector`(T4) and `seedOutboundCreate`(T5) — full connectors suite re-run GREEN at 75.3s incl. T4 tests, no regression). **US4 T6 DONE** (commits `ca79b92` feat + `91b6b25` review-fixes, `a7j.4.6` CLOSED): **migration 0046** `CREATE OR REPLACE FUNCTION sync_inbound_external_issue(...)` (the 0042 DEFINER) with a conflict-detection block BEFORE the ticket upsert — when the prior external status (`connector_sync_state.snapshot->>'status'`) exists AND native `ticket.status` diverged from the prior-external mapping AND incoming external differs from prior-external AND the new mapped status differs from current native, it INSERTs an `audit_entry` (`action='connector.conflict.resolved'`, `decision='external_wins'`, old=`{status:<native>}`, new=`{status:<mapped>,external_status:<raw>}`, `actor_principal_id=NULL`). External-wins behavior UNCHANGED; `inbound_sync.go` already passes a snapshot with `status` (no change needed). `0046…down.sql` restores the 0042 body verbatim. New `internal/connectors/conflict_integration_test.go::TestInboundConflictAudited` (sync 'To Do' → operator closes locally → sync 'In Progress' = both diverge → exactly 1 conflict audit + external wins status='open' → 3rd clean sync 'Done' → still 1 audit). Review fixes (`91b6b25`): captured ticket id in the conflict SELECT to drop a redundant correlated `INSERT…SELECT FROM ticket`; `syncIssueConflict` returns the uuid (dropped `ticketIDByExternal`); added a status-mapping drift comment. spec-compliance ✅ + code-quality ✅ APPROVED. **US4 T7 DONE** (commit `c66c05c`, `a7j.4.7` CLOSED): MF-004-US4 security regression pins. New `internal/security_regression/mf_004_us4_outbound_test.go` (5 infra-free `os.ReadFile`+`strings.Contains` source pins, NO build tag, runs under `make test` + `make sec-test`): (1) SSRF-safe outbound — `outbound.go` builds clients via `Registry.BuildSystem` (netsafe), no raw `http.Client{`/`http.DefaultClient`; (2) outbound writes audited — 0045 emits `connector.outbound.commented` + `connector.outbound.created`; (3) conflict audited — 0046 emits `connector.conflict.resolved` + `external_wins`; (4) create-issue ownership pushed into SQL — `EnqueueOutboundCreate` INSERT…SELECT scoped by `business_id` + `connector_id IS NULL` (foreign/unknown/already-linked → 0 rows), pinned via the create-unique INSERT column tuple so the assertion is create-specific; (5) service no-oracle — `EnqueueOutboundCreateIssue` maps 0-rows→`errs.ErrNotFound`, no `ErrForbidden` branch. Plus co-located behavioral pin `internal/connectors/mf_004_us4_outbound_pin_integration_test.go` (`//go:build integration`, package `connectors`, calls unexported `dispatchOnce`): `TestMF004US4_OutboundRefusesMetadataDial` — seeds a loopback connector then `UPDATE`s `base_url` to `169.254.169.254` (bypassing the create-time `validateBaseURL` SSRF layer #1) to isolate the **dial-time** guard (layer #2); `dispatchOnce` attempts the dial, netsafe refuses it (`blocked address 169.254.169.254`) even with `AllowPrivateBaseURL=true`, op requeued, message `external_id` stays NULL (no write-back). spec-compliance ✅ (traced the full dial chain, confirmed non-vacuity) + code-quality ✅ APPROVED (1 Minor — OWNERSHIP `business_id` literal was shared with the comment query → fixed by anchoring on the create-unique INSERT tuple, amended into `c66c05c`). Source pins 5/5 PASS; behavioral pin PASS (3.67s); `make sec-test` green. **Next increment = US4 T8** (`a7j.4.8`, end-to-end bidirectional round-trip integration test against an httptest Jira stub), then the US4-close gate; follow-up `a7j.4.9` (stale-op reaper). US5, US6 after. **NOTE: loop is stopped — increments are being driven interactively; confirm with the user before auto-continuing.**

### US4 plan — design decisions LOCKED (read the plan's "Design decisions" §; reviewers must validate vs spec §5.3/§7)
- **Outbound = poller + `connector_outbound_op` queue table, NOT a Bus subscriber.** Honors US4 bd-note (b) no-tx-across-HTTP (a Bus subscriber structurally holds the savepoint tx across HTTP). Mirrors `Reconciler`. Producer (`Reply`) inserts the op in the source tx (satisfies §5.3 intent w/o a generic outbox row). `TopicConnectorOutboundSync` stays unused (follow-up to remove).
- **Outbound dispatch is ADDITIVE — native email NOT suppressed** for connector-linked replies (product call deferred to follow-up `a7j.8`).
- **Idempotency anchor = the native row's `external_id`** (skip POST if already stamped). Residual crash-after-POST dup window documented, not closed (no Jira idempotency key).
- **"New linked ticket → issue" = explicit escalation** (`connectors.Service.EnqueueOutboundCreateIssue`) because there's NO native ticket-create path AND `ticket_connector_external_chk` forbids linked-but-unpushed; dispatcher's create branch sets connector_id+external_id+external_url atomically.
- **Migrations 0045** (op table + `connector_outbound_context`/`claim_outbound_ops`/`complete_outbound_comment`/`complete_outbound_create`/`fail_outbound_op` DEFINERs) **and 0046** (CREATE OR REPLACE `sync_inbound_external_issue` → audit both-changed conflicts as `external_wins`).

## Autonomous loop cadence (one fully-reviewed increment per iteration)
This file drives a **fresh-context** loop: `scripts/ralph-loop.sh` spawns a SEPARATE `claude -p` process per increment, so YOUR context starts near-empty each time — keep it that way (well under 30%; lean on subagents + this file, don't re-read everything). Read this file + `bd show manyforge-a7j`, perform exactly ONE increment, commit, then **EXIT** (do NOT start a second increment — the driver launches a fresh process for the next). Pick the lowest-numbered OPEN story (US3 `a7j.3` ✓closed → US4 `a7j.4` → US5 `a7j.5` → US6 `a7j.6`). Three increment types by state:
- **No plan yet** for that story under `docs/superpowers/plans/` → create one (writing-plans cadence; design doc is the spec below; brainstorming already done). First dispatch a subagent to extract the EXACT signatures you'll need (don't guess), then write a no-placeholder TDD plan to `docs/superpowers/plans/2026-06-07-us<N>-<slug>.md`, commit `--no-verify`, update this file. Also create the per-task bd children if the story needs them. One increment.
- **Plan exists, task unfinished** (check `git log --oneline -40` for committed tasks tagged `(manyforge-a7j.<N>)` + the plan's `- [ ]` boxes) → implement the next task subagent-driven: fresh implementer subagent (TDD → commit `--no-verify`, NO `Co-Authored-By`) → spec-compliance review subagent → code-quality review subagent → triage findings RIGOROUSLY (verify each; reject noise, accept sound fixes) → implementer fixes via SendMessage → re-run code-quality review. Tick the plan box, update this file. One increment.
- **All tasks committed, story not bd-closed** → run the FULL GATE: `export PATH=$PATH:$HOME/go/bin`; `gofmt -l internal/ cmd/ db/` (empty); `make test && make contract-test && make lint && make sec-test && make int-test` (int-test backgrounded ~7min). Green → final whole-story review subagent (opus). READY TO MERGE → `bd close manyforge-a7j.<N>`, file follow-ups, commit `.beads/issues.jsonl`, `git push`, update this file. Red gate → FIX it (no "pre-existing failures"). One increment.

**HARD RULES:** one increment then EXIT; NEVER skip the two reviews; NEVER bd-close on a red gate; NEVER push red; always leave this file current so the next fresh process resumes cleanly. **DONE = US4+US5+US6 all bd-closed AND `git status` up-to-date with origin** (the driver detects this via `bd list --status closed` and stops).

## Resume here
**Implement US4 = `manyforge-a7j.4` (Jira outbound), Task 8 (the LAST US4 task).** Plan exists, T1–T7 done → **"Plan exists, task unfinished"** increment. ⚠️ **The Ralph loop is STOPPED (manual control).** If a human is driving, confirm before auto-continuing; if you are the loop resumed via `./scripts/ralph-loop.sh 40`, just proceed. Concretely:
1. Read `docs/superpowers/plans/2026-06-07-us4-jira-outbound.md` (the **Task 8** §, ~line 1513) + the plan's `- [ ]` boxes. T1–T7 boxes are all `[x]`; **Task 8 is the lowest (and last) unfinished**.
2. **Task 8 (end-to-end bidirectional round-trip integration test).** `bd update manyforge-a7j.4.8 --claim`, then invoke `superpowers:subagent-driven-development`: fresh implementer subagent (TDD → commit `--no-verify`, NO `Co-Authored-By`) → spec-compliance review → code-quality review → triage findings RIGOROUSLY (trust `go build`/`go test` over gopls dbgen/integration false positives) → fixes via SendMessage → re-review.
   - T8 specifics (read the plan's Task 8 § for the exact assertions): a single `//go:build integration` test exercising the full loop against an httptest Jira stub behind the netsafe SSRF client — inbound sync creates/links a ticket, a native reply enqueues + dispatches an outbound comment (external-id write-back), and a both-changed inbound update finalizes external-wins + audits the conflict. Reuse the existing connectors test harness (`startConn`, `seedOutboundConnector`/`seedOutboundCreate`, `registerStubJira`, the conflict + outbound integration tests as references). This is the capstone integration coverage, not new product code.
3. Tick the plan's Task 8 boxes, close `manyforge-a7j.4.8`, update this file (mark T8 done). **THEN all 8 core US4 tasks are committed → the NEXT increment is the US4-close gate** (full gate: `gofmt -l` empty + `make test && make contract-test && make lint && make sec-test && make int-test` → final whole-story opus review → `bd close manyforge-a7j.4`, file follow-ups, commit `.beads/issues.jsonl`, **`git push`** at this gate). EXIT after T8. One task = one increment.

**US4 task list (plan = 8 tasks):** T1 CreateIssue interface+Jira client ✅(170e929) · T2 migration 0045 op table + DEFINERs ✅(d997963) · T3 producer hook in `ticketing.Reply` ✅(cd587b1) · T4 OutboundDispatcher poller + main wiring ✅(69264a8) · T5 EnqueueOutboundCreateIssue + create-issue dispatch ✅(59f3dbb) · T6 migration 0046 conflict audit ✅(ca79b92+91b6b25) · T7 MF-004-US4 security pins ✅(c66c05c) · **T8 bidirectional round-trip integration ⬅NEXT (last US4 task)**. Plus follow-up **`a7j.4.9`** (stale-`in_progress` reaper, deferred from T4). When all 8 core tasks committed → run the FULL GATE → final opus review → `bd close manyforge-a7j.4`.

US4 scope (spec §8 US4): native reply→Jira comment; new connector-linked ticket→Jira issue (via explicit escalation, see plan D4); external-id write-back; conflict finalized (external-wins, both-changed audited); full round-trip integration test against an httptest Jira stub behind the SSRF netsafe client.

## Run & verify (full gate, all GREEN at c43ee5c)
- **Pre-flight:** `export PATH="$PATH:$HOME/go/bin"` (golangci-lint only there — else `make lint` silently degrades to vet-only). Docker up.
- **Full gate:** `gofmt -l internal/ cmd/ db/` (must be empty) `&& make test && make contract-test && make lint && make sec-test && make int-test`. `int-test` ≈ 6–7 min (run it backgrounded). `make generate` runs `sqlc generate` (the ONLY generator) after editing `db/schema.sql`/`db/query/`.
- **Connectors-only fast loop:** `go test -tags integration -p 1 ./internal/connectors/ -v` (needs Docker; testcontainers auto-applies `migrations/`).
- Dev DB (optional, for `migrate` round-trip checks): owner DSN `postgres://manyforge:devpassword@localhost:55432/manyforge?sslmode=disable` (SSH tunnel; not started by this session).

## Gotchas (don't relearn these)
- **gopls LIES persistently** on every file referencing `dbgen` ("undefined: dbgen.X") and on every `//go:build integration` file ("No packages found"). These are FALSE POSITIVES — `go build ./...` is clean and tests pass. **Trust `go build`/`go test`, NEVER the IDE diagnostics panel.** (Fired on every single task in US1+US2.)
- **dbgen nullable nuance:** with the `uuid`/`timestamptz` go_type overrides, `emit_pointers_for_null_types` does NOT apply to them. `<col> uuid NULL` → `pgtype.UUID` (NOT `*uuid.UUID`); `text NULL` → `*string`; `timestamptz NULL` → `pgtype.Timestamptz`. Plain `text NOT NULL` → `string`. (`ticket.ConnectorID` is `pgtype.UUID`, `ticket.ExternalID`/`ExternalUrl` are `*string` — US3 reads/writes these.)
- **Two casings:** Go service structs use `BaseURL`/`AllowPrivateBaseURL`; generated dbgen fields use `BaseUrl`/`AllowPrivateBaseUrl` (Url, not URL). Match each layer.
- **Composite-FK discipline:** every new child table needs `business_id`+`tenant_root_id`, `FOREIGN KEY (x_id, tenant_root_id) → parent(id, tenant_root_id)`, a `*_troot_immutable` trigger, RLS (`authorized_businesses(current_principal())`), GRANT to `manyforge_app`. Mirror every column into `db/schema.sql` (strip DEFAULT/RLS/trigger). Reuse `support_tenant_root_immutable()` (from 0013, don't redefine).
- **Push ownership predicates into SQL:** US1/US2 both gained a `EXISTS (SELECT 1 FROM <parent> WHERE id=$ref AND business_id=b.id)` guard in INSERT…SELECT (same-business defense-in-depth beyond the same-tenant FK). US3's upserts should do likewise.
- **Commits use `--no-verify`** (intermediate task commits; full gate runs at US-close). **NO `Co-Authored-By`** (user rule). The **bd hook re-exports `.beads/issues.jsonl` on commit** — stage it. `sqlc`/`migrate` on standard PATH; `golangci-lint` only `~/go/bin`.
- **Spec branches STACK:** master is ~141 commits behind; `004-external-connectors` branched off `003-agent-runtime` (not merged to master). Branch from current HEAD; don't merge to master.
- **Next migration: 0046** (0040 US1, 0041 US2, 0042/0043/0044 US3, **0045 US4 T2 = outbound op table + DEFINERs ✓; EXTENDED in T4** — `claim_outbound_ops` now RETURNs post-increment `attempts`, and a `message_external_id(uuid)` DEFINER was added; down-migration kept symmetric). US4 T6 uses 0046 (conflict-audit `CREATE OR REPLACE sync_inbound_external_issue`). NOTE: 0045 is unpushed/never-applied-to-a-real-DB on this branch, so editing it in-place (vs a patch migration) is correct here — testcontainers re-applies `migrations/` from scratch. External webhook ingest route is PUBLIC (no JWT) — mirror `internal/inbox/handler.go`'s rate-limit-before-routing + uniform-202 no-oracle pattern.

## Decisions & rationale (brainstorming forks, all locked)
- **Jira FIRST** end-to-end, then Zendesk as a thin 2nd (US5). **API-token auth** (email:token HTTP Basic); OAuth2 deferred. **External-wins** scalar conflict policy (Jira authoritative; ManyForge mirrors/triages); comments append-only union deduped by external id; deliberate gated writes are the separate authoritative path. **US6 agent tools = read + gated write** (add_external_comment / transition_external_status). On-prem Jira via the per-connector `allow_private_base_url` trust flag (deo.9 pattern).
- **US3 open design decisions** (on `a7j.3` notes): where the per-connector reconcile cursor lives (MAX(external_updated_at) over sync_state, or a new `connector.last_reconciled_at`); whether to widen `ListUpdatedSince` from `[]string`; enqueue `connector.inbound.sync` in the SAME tx as the dedupe insert.

## Next steps
1. **US3** (`a7j.3`) — Jira inbound (this resume). 2. **US4** (`a7j.4`) — Jira outbound + full bidirectional + conflict finalize. 3. **US5** (`a7j.5`) — Zendesk thin 2nd. 4. **US6** (`a7j.6`) — read + gated-write agent tools. 5. Follow-up **`a7j.7`** — `UpdateConnectorCredential` must re-validate+re-audit the trust flag. Then close epic `a7j`.

## Pointers
- **Spec (master design):** `docs/superpowers/specs/2026-06-06-external-connectors-design.md` (§2 framework, §3 data model, §5 sync engine, §7 security pins, §8 US breakdown).
- **Plans:** US1 `docs/superpowers/plans/2026-06-06-us1-secrets-vault.md`; US2 `docs/superpowers/plans/2026-06-07-us2-connector-framework.md`; US3 `docs/superpowers/plans/2026-06-07-us3-jira-inbound.md`; **US4 `docs/superpowers/plans/2026-06-07-us4-jira-outbound.md` (current, 8 tasks).**
- **Key code (US1+US2):** `internal/connectors/` — `connector.go` (`TicketingConnector` interface + `ExternalIssue`/`ExternalComment`/`WebhookEvent` + sync topics), `registry.go` (`Registry`/`Factory`), `service.go` (`Service.Create`/`Resolve`/`mapErr`), `credential.go` (`validate`/`validateBaseURL`), `sync_store.go` (`RecordWebhookDelivery`), `types.go` (`ResolvedConnector`/`Credential`/`CreateConnectorInput`/`Verifier`). Vault: `internal/platform/secrets/vault.go`. Schema: `migrations/0040_*`, `migrations/0041_connector_sync.*`, `db/query/connector.sql`. Test helpers (reuse in US3): `internal/connectors/{credential_test.go,testsupport_integration_test.go}` → `startConn(t)`, `newConnService(t,tdb,v)`, `jiraInput()`, `seedConnectorTenant(ctx,t,tdb)`, `fakeConnector`.
- **Cadence skills:** `superpowers:writing-plans` → `superpowers:subagent-driven-development` (fresh implementer per task; spec-compliance review THEN code-quality review; opus for the final whole-US review). Track work in **bd** (`bd ready`, `bd show <id>`, `--claim`, `bd close`), NOT TodoWrite.
- **bd:** epic `manyforge-a7j` (`bd show manyforge-a7j` for the tree). Resume command: `/handoff resume`.
